# Legacy and Backwards-Compatibility Audit

Three audits, twelve lenses, seventeen waves of cleanup — all landed as of 2026-08-07. The first audit (2026-08-06) swept the tree for obvious dead code (Waves 1-5). The second (2026-08-07) reorganized by *kind* of legacy rather than region of tree — twelve independent lenses — and found roughly 100 items the first never saw (Waves 6-12). The third (2026-08-07) re-ran the lenses with per-finding adversarial verification: 96 findings, 93 confirmed, 2 refuted, 1 routed to a decision, collapsing after cross-lens dedup to 64 mechanical removals (Waves 13-17, net −742 lines). Everything mechanical is done. The drift mechanism previously flagged as unguarded (end of §2) turned out to be already closed by commit 2862a6e6; what remains actionable is §3, now a decided implementation plan — eighteen independent waves, one per decision, each implementable in isolation. Decision waves 1 through 11 landed 2026-08-07; seven remain open.

The durable finding was never the dead code — it was **drift**: hand-maintained lists that silently stopped mirroring their registries, docs prescribing env vars the CLI no longer reads, schemas documenting capabilities never implemented. Dead code is harmless until someone reads it; drift actively misleads. Every user-visible defect these audits found was drift, and §2 lists the guards that now make each class of it self-detecting.

## 1. Completed — the short record

- **Waves 1-5** (first audit): obvious dead code across `src/`, `test/`, `scripts/`, `package.json`, `Dockerfile`.
- **Waves 6-12** (second audit): seven user-visible drift defects, each fixed by deriving the drifted list from its registry instead of patching it (§2); ~40 mechanical deletions; test-runner report plumbing rewritten to the selector spellings tests actually use; back-compat arms removed one commit per item; the help flag-group catalog renamed to `HELP_FLAG_GROUPS`, derived, and pinned by a test that caught a fourth stale key on its first run; ADRs 001-005 annotated as history rather than rewritten; ADR-018's unimplemented retired-pricing clause closed with `RETIRED_MUSIC_MODEL_RATES`; the naming lens closed with the format-neutral `chapterFiles`/`chapterChunkLimitChars` rename across 37 sites.
- **Waves 13-17** (third audit, 64 items): dead exports, files, and directories (13); options no caller passes and the unreachable branches they feed (15); registry schema fields, dead type surface, and prompt plumbing nothing reads (12); the CLI surface — the `ttsFlags` sweep from 72 definitions to the 21 the parser actually registers, plus two unreachable parser/benchmark arms (3); test residue, docs drift, and infra (21). Every wave passed the §2 bar, and the batch closed with a per-item adversarial re-verification of the applied diff: every removal complete, zero residue in any spelling, zero collateral edits.
- **Decision waves 1-2** (§3, landed 2026-08-07): `mistral-ocr-latest` removed from `SUPPORTED_MISTRAL_OCR_MODELS`, from its byte-for-byte-duplicate registry row, from the OCR command doc, and from every one of the eight test sites that accepted it — the single surviving mention is the rejection pin now guarding its return — with ADR-011 annotated; and the unverified `tts-1`/`tts-1-hd` retirement question recorded in both `pricingNotes` rows and as an ADR-018 follow-up action. `bun test test/test-cases/validation/` held at 1248 pass across 182 files.
- **Decision waves 3-5** (§3, landed 2026-08-07): Speechify's unread `voices` array deleted from its registry row; the `costPer1kOutputChars` chain removed end-to-end across nine sites in one change; and llamafile given the config destination its `--llm` help already advertised, with a new `llm-defaults.test.ts` whose third case derives the check from `WRITE_LLM_PROVIDER_TARGETS` so no future `--llm` provider can drift the same way. Suite now 1251 pass across 183 files.
- **Decision wave 6** (§3, landed 2026-08-07): all 68 model validators stopped naming internal target flags in their rejection messages, fixed centrally in `model-validation.ts` with zero call-site churn, and pinned by a new derived guard. Suite now 1255 pass across 184 files.
- **Decision waves 7-8** (§3, landed 2026-08-07): the standalone `image` command's rename map became the single home for its two spellings and now retargets usage errors as well as help text, closing a drift the audit had undercounted by seven sites; and `--tts-voice` typed alongside dialogue flags became a hard error instead of a silent discard. Suite now 1260 pass across 185 files.
- **Decision wave 9** (§3, landed 2026-08-07): `--tts-speaker` mappings became the sole multi-speaker switch, so a `ttsDialogueFormat` inherited from config defaults warns and continues single-speaker instead of aborting step 4 after three paid steps; the shared `assertDialogueFormatIsUsable` gives the typed flag a hard error and the config default a warning from one home, and two throws the narrowing made unreachable were removed. Suite now 1262 pass across 185 files.

- **Decision wave 10** (§3, landed 2026-08-07): the `'x-space'` route overload got a name and a written reason without moving a persisted byte — `URL_ARTICLE_ROUTE` now stands in for thirteen of the fourteen route literals across the two resume files, and `ExtractRoute` itself carries the comment explaining why the string cannot change. Behavior-neutral, so the suite held at 1262 pass across 185 files.

- **Decision wave 11** (§3, landed 2026-08-07): the URL article capability seam — a fifteen-member capability union, six per-adapter capability arrays, a rejection helper, and twelve option fields no caller could ever set — deleted end to end, taking the two unreachable request-body mapping blocks and one orphaned helper with it; the two conditionals it left behind collapse to literals that existing tests already pin, so a −248-line removal needed no new guard. Suite now 1261 pass across 185 files (one fewer: the capability pin test itself).

Three lessons that outlived their waves:

- A `[verified]` mark records that a finding's *observation* was confirmed, never that its *prescription* was — four Wave 10 items would have made things worse applied as written (a new dead alias, a regressed warning suppression, a fleet of non-resumable manifests). Decision wave 1 hit a milder form of the same edge: four of its eight prescribed retargets would have restated something the file already contained verbatim, so they became deletions plus one strictly stronger rejection pin.
- Never bump an artifact `schemaVersion` without shipping the upgrader in the same change: four hard `schemaVersion !== 2` rejects exist with no migration code anywhere, so a bump alone makes every manifest on disk non-resumable.
- Dead *symbols* and dead *reachability behind live symbols* are different classes: optional parameters no caller passes survive symbol-level sweeps and need per-option call-site enumeration to find (all of Wave 14 was this class, which is why it survived two audits).

## 2. Verification bar and guards

Per change: `bun run check`, then the no-cost smoke set (`cli-help-contracts`, `cli-usage-errors`, `option-resolution-contracts/`). `bun test test/test-cases/validation/` is green (1261 tests across 185 files); treat any failure as real — the old standing allowance for pre-existing failures was withdrawn 2026-08-07. One known exception, unrelated to any wave: `process-lock-contracts.test.ts`'s "process lock serializes separate processes" is timing-sensitive and flaked once in four full-suite runs on 2026-08-07. Never run paid-provider commands to verify anything in this document; paid e2e suites cited as pins are read-only citations, not instructions to execute.

Shipped guards, each replacing a hand-maintained mirror with a derived source or a two-direction pin:

- `registry-derived-config-keys.test.ts` — every step-2 registry `configPath` parses under the strict config schema.
- `getHostedProviderEnvKeysForConfigPrefix` — setup's credential catalogs are derived, not typed.
- `help-flag-groups.test.ts` — declared help groups and claimed help groups are equal in both directions.
- `buildConfigPatchFromFlags`'s discarded-flag warning — a flag with no config destination says so instead of succeeding silently.
- `explicit-runtime-exclusions.test.ts` — `RUNTIME_ONLY_FLAGS` stays disjoint from `FLAG_TO_CONFIG_PATH`, with `prompt`'s premise pinned separately.
- `RETIRED_MUSIC_MODEL_RATES` — retiring a priced model means moving its rate to a historical table, not deleting it.
- `links-registry-contracts.test.ts` plus the derived `fixtures/registry.ts` — links fixture expectations are read from the `model-links/*.json` registries at runtime instead of hand-maintained (see below).
- `llm-defaults.test.ts`'s "every `--llm` provider target has a config destination" — the selector table and `FLAG_TO_CONFIG_PATH` must agree, so a provider the help advertises cannot silently save nothing.
- `model-selector-messages.test.ts` — every `createModelValidator` key (collected at construction, so the list cannot be hand-maintained) resolves to a public selector, and every provider target round-trips to its own selector. A model validator whose provider is missing from the public selector tables fails here instead of shipping an error naming a flag nobody can type.
- `image-command-flag-spellings.test.ts` — `imageCommandOptionNames` and the flags the `image` command actually registers must agree in both directions, every internal spelling must retarget to exactly its public one (so a key that is a prefix of another key cannot silently rewrite it), and two real step-5 rejections are retargeted end to end. The rename map is the only home for the two spellings, so help text, argv normalization, and error text cannot drift apart.

**Formerly unguarded, now closed.** The `links` fixture constants in `test/test-cases/validation/content-output/metadata-links-lyrics-contracts/` were flagged here as the last unguarded drift mechanism, but that claim was stale on arrival: commit 2862a6e6 (2026-08-07) had already converted every fixture constant to runtime derivation — `provider-selector-groups/fixtures/registry.ts` reads the `model-links/*.json` registries from disk — and added `links-registry-contracts.test.ts` pinning registry integrity (unwired manifests, duplicate provider owners, cross-section URL dupes, absolute-URL form). The accepted residual: expectations and production read the same JSON, so a truncated or wrong-URL registry propagates silently; add a small hand-pinned sentinel (a few stable canonical URLs) only if registry truncation becomes a credible failure mode. A generator writing literal fixtures would be strictly worse — it reintroduces the on-disk copy that caused the drift in the first place.

## 3. Decided — one wave per decision

The ranked-options revision of this section is superseded (it survives in git history): the recommended option for every item is now the decision, and each decision below is a wave of its own. The wave counter restarts at 1 — Waves 1-17 in §1 are the earlier, closed mechanical series. These waves are independent: none depends on another, they can land one by one in any order, and each passes the §2 bar (`bun run check` plus the no-cost smoke set) before the next begins. No wave requires a paid-provider call to implement or verify. File:line references were verified 2026-08-07, and are stale for any file a landed wave has since touched.

Waves 1 through 11 landed 2026-08-07 and are kept below as records rather than deleted, so the numbering of Waves 12-18 stays stable. Seven waves remain open.

**Provider and model catalog**

**Wave 1 — Drop the `mistral-ocr-latest` alias. Landed 2026-08-07.** The no-moving-aliases policy in ADR-011/018/019 stood; the alias duplicated `mistral-ocr-4-0` byte-for-byte and made `--all-ocr` pay for the same model twice. `'mistral-ocr-latest'` is gone from `SUPPORTED_MISTRAL_OCR_MODELS` and its row is gone from `ocr-config/ocr-mistral.json`. Of the eight prescribed test sites, two retargeted to `mistral-ocr-4-0` verbatim (`mistral-rest-contracts.test.ts`, and `model-calibration.test.ts`, whose synthetic fixture description moved with the key); two were enumerations that simply lost a member (`provider-expansion-concurrency.test.ts:491`, and `ocr-pricing.test.ts`, whose two-target case became one target with `toHaveLength(1)` and `totalCost` 0.8); and four would have restated something the file already held verbatim, so they were deleted instead — `cli-usage-errors.test.ts:412` behind an existing `mistral-ocr-4-0` case, `provider-expansion-concurrency.test.ts:76` behind an existing acceptance, that file's `mistralLatestOcrOpts` pair whose `buildOptsFromFlags` call was identical to `mistralOcr4Opts`, and `price-commands/registry/ocr.ts:8` whose args matched the `mistral-ocr-4-0` command directly above it. To recover the coverage those deletions would have cost, `provider-expansion-concurrency.test.ts` gained a rejection pin on the full error string beside the existing `mistral-ocr-2405` one — a stronger guard than any of the retargets it replaced, since it fails if the alias is ever reintroduced. `03-extract-ocr.md` now states the alias is not accepted; ADR-011 carries the removal as a dated follow-up naming it the last selector contradicting that ADR's own "no `*-latest` alias" clause. Users who type the alias get the standard invalid-model error listing the concrete IDs, as intended.

**Wave 2 — Pin the `tts-1`/`tts-1-hd` re-check. Landed 2026-08-07.** Both rows kept; provider-side retirement is unverified and may not be verified by calling an API, so the open question was recorded exactly where the next refresh will read it. Both `pricingNotes` rows in `tts-config/tts-openai.json` now end with "Previous generation; verify provider retirement status at the next hosted-TTS model refresh.", and ADR-018 gained a Follow-up Actions row stating the question must be answered from OpenAI's published model documentation rather than by calling the API. `tts-models.ts` untouched; no code or test changes, as decided.

**Wave 3 — Delete Speechify's dead `voices` array. Landed 2026-08-07.** The TS constants stay the single home for the voice catalog, since they encode allow/deny behavior the flat registry list cannot: the deleted twelve-entry array was a flat merge of `SPEECHIFY_SIMBA_3_2_BUILT_IN_VOICES` and `SPEECHIFY_KNOWN_INCOMPATIBLE_BUILT_IN_VOICES`, which the merge erased the distinction between. The `voices` key is gone from `tts-config/tts-speechify.json`; the consumed `models` pricing block is untouched. No reader existed (`model-loader/tts.ts` exposes voice accessors only for kitten/groq/grok) and `voices` is optional in `TtsServiceSchema`, so validation and behavior are unchanged.

**Wave 4 — Remove the `costPer1kOutputChars` chain atomically. Landed 2026-08-07.** All nine sites went in one change, as prescribed: the two schema fields in `ExtractModelSchema`, the return-type field and converter read in `getExtractPricing`, the type fields on `ExtractStepEstimate` and `EstimatedStepEntry`, the `preflightToEstimated` copy, both `reporter.ts` arms, the `buildRatesUsed` spread in `ocr-costs.ts`, the `formatRatesSummary` `/1k chars` arm, and the two allowlist entries in `registry-provenance.test.ts`. Unproducibility was re-confirmed before the cut: no registry JSON carries either spelling, and no consumer spreads `getExtractPricing`'s result wholesale — every reader names the fields it copies, so the removed key could never reach an estimate. If a per-character-priced extract provider ever ships, rebuild mechanically from this list or git history.

**CLI surface**

**Wave 5 — Give llamafile a config destination (parity with `llama`). Landed 2026-08-07.** All five prescribed edits landed: `'llamafile'` added to `LLM_PROVIDER_FLAGS`, to the `d.llm` injectProviderGroup entries, and to `FLAG_TO_CONFIG_PATH` (→ `['defaults','llm','llamafile']`); `llamafile: ModelArraySchema` added to `LlmDefaultsSchema`; `'llamafile'` added to `REPEATABLE_MODEL_FLAGS` beside `'llama'`. Both halves were required together — the path entry alone would have made bare `config --llm llamafile` persist `true`, which `ModelArraySchema` then rejects on read; membership in `REPEATABLE_MODEL_FLAGS` routes it through `normalizeModelFlagOccurrences` instead. New `config-contracts/llm-defaults.test.ts` covers the single-model persist plus `loadConfig` round-trip, the repeated-selector array case, and a derived guard asserting every `WRITE_LLM_PROVIDER_TARGETS` value has a `FLAG_TO_CONFIG_PATH` entry. Residual asymmetry, deliberately out of scope: `llama` has a `DEFAULT_LOCAL_MODEL_BY_FLAG` entry so bare `--llm llama` resolves a default model, while `llamafile` has none, so bare `--llm llamafile` still selects nothing (unchanged from before this wave, in both the config and write paths).

**Wave 6 — Truthful model-validation errors. Landed 2026-08-07.** `createModelValidator` now formats through `describeModelSelector`, which splits the suffixes `-stt`/`-ocr`/`-tts`/`-image`/`-video`/`-music` into provider plus category and reads the public spelling back out of the seven target maps in `provider-targets.ts` (a leaf module with no imports, so the new dependency from `model-validation.ts` is cycle-free). Both spellings are named, per `target-validation.ts`'s convention: `--provider/--ocr mistral[=model]` for the six categories, `--llm cerebras[=model]` for bare LLM keys. `llamafile` needed no override after Wave 5 put it in `WRITE_LLM_PROVIDER_TARGETS`, so the override map holds exactly two entries — `whisper` (predates the suffix convention) and `whisperfile` (also extract-only, so its message must not advertise an `--stt` spelling that does not exist). The message shape moved from `Invalid --kimi-ocr model "X". Allowed values: …` to `Invalid model "X" for --provider/--ocr kimi[=model]. Allowed values: …`; the five `extraMessage` clauses that said "This flag" now say "This selector", since two flags are named. All 68 keys resolve with zero fallbacks (verified by enumeration), and the hand-written `validateLlamaModel` message in the same file was retargeted through the same formatter rather than left as the one surviving `--llama`. Pin updates: 23 literal `Invalid --<flag> model "<X>"` strings retargeted mechanically across five test files; the two loops keyed by internal flag (`model-flags-and-ordering.test.ts`, `tts-request-controls.test.ts`) now derive their expectation from `formatModelSelector`, keeping the flag-to-validator binding the old assertion had; four partial pins the `model` grep missed were fixed by hand, including `bfl-recraft-image-options.test.ts:41`, which pinned only `Invalid --recraft-image` and so was caught by the suite rather than the grep. Residual, deliberately out of scope: `setup --models whisperfile:bogus` names `--provider whisperfile[=model]` because the validator has no surface context — a real spelling that accepts that model, but not the one the user typed.

**Wave 7 — Bless the image command's prefix drop; remove the residue. Landed 2026-08-07.** The rename stood, as decided. `imageCommandOptionNames` is exported and `define-image-command.ts`'s byte-for-byte-duplicate `IMAGE_COMMAND_OPTION_FLAGS` is gone, so argv normalization and the flag definitions read the same map. The error-spelling half was larger than the finding stated: `--image-search-grounding` is hardcoded at nine sites, not two — the `IMAGE_OPTION_LABELS` entry plus seven per-provider `unsupported.push` calls and a Gemini list — and *every* `--image-*` label was wrong on this surface, not only that one, which three e2e files had pinned in nine assertions (`--image-aspect-ratio is not supported by BFL/flux-2-pro` for a user who typed `--aspect-ratio`). Retargeting each site individually would have left the prose clauses (`Supported Grok image options: --image-count, …`) still wrong, so the fix is one boundary retarget instead: `renameFlagSpellings` lifted out of `renameFlags` in `flag-utils.ts` (help text and error text now share one derived translation), applied to `AppUsageError` messages at the `image` command boundary. The handler body moved into `runImageCommand` so the wrapper is a five-line `try`/`catch` rather than an 88-line reindent. All nine e2e assertions were retargeted to the short spellings, `cli-usage-errors.test.ts` gained a two-case CLI-level pin, and `image-command-flag-spellings.test.ts` (§2) is the derived guard. Deliberately untouched: the seven `unsupported.push('--image-search-grounding')` literals, since `collectUnsupportedCommonFlags` tests `!== undefined` and the `=== true` guards defend against a `false` reaching it from a resume path; `--image-provider-concurrency`/`--image-local-concurrency` are absent from the rename map and no error names them. The spelling note landed in all three standalone command docs (image, video, music), each citing ADR-012.

**Wave 8 — Hard error for `--tts-voice` in dialogue mode. Landed 2026-08-07.** The throw sits between `buildOptsFromFlags` and `collectTtsTargets` in the tts command handler, so it fires before preflight on both the single-file and directory paths, and the prescribed message shipped verbatim with a closing period, matching `target-validation.ts`. `TTS_GENERIC_OPTION_TARGETS['tts-voice']` is now exported as the derived `TTS_VOICE_OPTION_TARGETS`, so a twelfth provider voice target is covered without a second list. Dialogue presence reuses `isMultiSpeakerRequested` rather than re-reading the flags, which keeps the check and the mode switch in sync — including through Wave 9, which narrows that predicate to speaker mappings and adds its own error for the format-only case. Gating on `explicitFlags` was load-bearing exactly as predicted: `ttsSpeaker` always resolves (kitten's `'Jasper'` default), so a resolved-value check would have rejected every dialogue run. The provider-specific voice flags are unreachable here anyway — the `tts` command registers only the generic `--tts-voice`, which `normalizeGenericTtsOptionFlags` rewrites into a provider target before the check reads `explicitFlags`. No existing test or e2e run combined the two flags, so nothing needed migrating; `--tts-ref-audio` is the same class of conflict and stays out of scope, as decided. Docs: the two `text-to-speech.md` clauses that said `--tts-voice` is *ignored* under speaker mappings now say it is rejected, and note the config-default exemption.

**Wave 9 — Multi-speaker derives from mappings only. Landed 2026-08-07.** `isMultiSpeakerRequested` is now `(options.ttsSpeakers?.length ?? 0) > 0`, and `resolveDialogueFormat`'s rule that speaker mode requires a format is untouched, so the two halves of dialogue mode became one switch plus one requirement instead of two independent triggers. The warn/error pair ships as one exported `assertDialogueFormatIsUsable(options, explicitFlags?)` beside the predicate rather than as two inline checks, which is what let both entry points share it: the `tts` command handler passes `explicitFlags` and gets the hard error, while `runTts` — the pipeline's step-4 entry, and the only caller of the two, since the standalone command runs through `runTtsForTargets` — passes none and gets the warning, so no run can both warn and throw. That placement is the actual bug fix: the motivating case was a `ttsDialogueFormat` in config defaults aborting step 4 after three paid steps, and it now logs and continues single-speaker. Two throws went unreachable, not one: the named duplicate in `normalizeDialogueText` was deleted outright (an empty registry degrades to the existing no-turns error, and production cannot reach it anyway — `run-multi-speaker-tts.ts` runs only for targets that carry a `multiSpeakerStrategy`, which requires mappings), and `target-validation.ts:25-27` — which the finding had not counted — became unreachable for the same reason, so its `CLIUsageError` was replaced by an `InternalError` that narrows the optional `speakerVoiceRegistry` and no longer advertises a user-facing error that cannot fire. `input-validation.ts:11`'s `isMultiSpeakerRequested(options) && (options.ttsSpeakers?.length ?? 0) > 0` collapsed to its first clause, the two halves now being the same expression. Tests: the `tts-dialogue-contracts.test.ts` case that pinned the old format-only rejection was repointed to the surviving requirement (mappings without a format still throw `Dialogue TTS requires --tts-dialogue-format screenplay|labeled.`), and a new case pins the whole switch — format-only yields one target with no `multiSpeakerStrategy`, the same options warn without `explicitFlags` and throw with them, and adding mappings silences both arms — plus a CLI-level pin in `cli-usage-errors.test.ts`. Wave 8's `--tts-voice` error is deliberately unchanged: it reuses the narrowed predicate, so it now fires only when mappings are present, which is the only case in which a typed voice is actually discarded. Docs updated in `text-to-speech.md` (flag table and dialogue prose) and `config.md`; the `--tts-dialogue-format` help now says it requires `--tts-speaker`. Suite now 1262 pass across 185 files.

**Architecture**

**Wave 10 — Name the `'x-space'` overload without touching persisted bytes. Landed 2026-08-07.** No rename and no `schemaVersion` bump, as decided; the four hard `schemaVersion !== 2` rejects still have zero migration code, so the split-plus-upgrader path stays recorded in ADR-002 as the end state if ever funded. `URL_ARTICLE_ROUTE = 'x-space' as const` is exported from `resume-registry.ts` — the module that *defines* the overload, since `getExtractRouteResumeHandler` is where the route keys `urlArticleResumeHandler`, and the module `resume-dispatch.ts` already imported, so the new dependency added no edge and no cycle. Thirteen literals moved onto it, not the seven the finding implied: three in `resume-dispatch.ts` (the `article` selector route, and both `inferExtractRoute*` returns) and ten across the four `resume-registry.ts` sites, because each of the three child-batch blocks names the route three times rather than once — `buildChildResumeTarget(dir, 'x-space', childBatches['x-space'])` spells it twice on one line. That second spelling is a persisted manifest key rather than an in-memory route value, and folding it in is the wave's one judgment call past the prescription: the constant is documented as the persisted literal, so the two meanings cannot drift apart without an explicit edit, and a future route split has to touch both anyway. The fourteenth literal stays bare deliberately — `isExtractRoute` (`resume-dispatch.ts:52`) tests the persisted route set, not the overload, so both new comments say so and name its sibling in `manifest-utils.ts:112` to keep the next reader from "fixing" it. The `ExtractRoute` comment carries findings 1 and 6 (the literals are persisted; no upgrader exists) alongside findings 2-3, since the reason *not* to rename is the more useful half to have at the declaration. ADR-002's finding 2 gained a dated line naming the constant, so the citation runs both ways. No new test, and none is missing: `resume-provider-surface-contracts.test.ts:348,395` already pass a bare `'x-space'` into `normalizeResumeSelectorFlagsForTarget` and assert URL-article routing out of it (`url-provider`, `all-local-url`), so changing the constant's *value* fails them from outside the module — which is the only failure mode worth pinning, the symbol being free to rename.

**Wave 11 — Shrink URL capability negotiation to what ships. Landed 2026-08-07.** The seam is gone: `url-provider-adapter.ts` deleted outright (67 lines, both `assertUrlArticleOptionsSupported` and `requiresCapability`), along with the `capabilities` field on `UrlArticleProviderAdapter`, the six `*_CAPABILITIES` arrays, and the six assert calls. Unreachability was re-confirmed before the cut, per the Wave 4 convention: the options object has exactly two construction sites — `buildUrlRunOptions` (`url-run-state.ts:44-49`) and the inline literal in `prepareArticleDocument` (`document-runner.ts:32-35`) — both setting only `timeoutMs` and `requestAttempts`, with `runUrlArticleProviderWithStats` adding `requestSignal`; all eight `runUrlArticleProvider*` call sites pass one of those two objects through unchanged, and nothing in the chain deserializes options from disk, so no capability field could ever be non-undefined and no rejection could fire. Two of the three prescribed option-mapping blocks existed and one did not: firecrawl lost `includeTags`/`excludeTags`/`waitFor`/`location`, spider lost `root_selector`/`exclude_selector`/`wait_for`/`country_code`/`locale` plus the now-orphaned `durationFromMs` helper the finding had not counted, but zyte has no mapping block at all — its body hardcodes `article: true`, so `'structured-extraction'` was a declared capability with no implementation behind it and removing the array is the whole of that item. The two conditionals that collapsed to literals (`onlyMainContent: options?.contentScope === 'full' ? false : true` → `true`, and spider's `filter_output_main_only` likewise) needed no new pin: `firecrawl-glm-spider.test.ts:57,185` already assert both request bodies with `true`, so behavior-neutrality was guarded from outside the change. One judgment call past the prescription: "narrow `UrlArticleRunOptions` back to `UrlRequestOptions`" was read as *deleting* the type rather than aliasing it, since with the extension fields gone the two are identical and `UrlArticleRunOptions = UrlRequestOptions` would have been exactly the dead alias Wave 10's lesson warns against — every site retargeted to `UrlRequestOptions` directly, which is why the change is 14 files rather than the estimated 9. Deleting the pin test left `getUrlArticleProviderAdapter` with no consumer in the test barrel, so its import and re-export went out of `shared.ts` too (the production export stays live at `prepare-html-article.ts:125`). No doc referenced the capability surface or any URL option flag, so nothing user-facing was ever promised and nothing needed retracting. Net −248 lines (+38/−286) across 14 files, one deleted; rebuild from this list or git history if the flags ever ship. Suite now 1261 pass across 185 files — one fewer than Wave 10, the deleted pin.

**Wave 12 — Derive `isLocalUrlBackend` from the registry.** One line, behavior-identical: change `url-targets.ts:5` to `(LOCAL_URL_ARTICLE_BACKENDS as readonly string[]).includes(backend)`, adding `LOCAL_URL_ARTICLE_BACKENDS` to the existing import from `../step-2-shared/provider-registry` (cycle-free — `url-providers.ts` imports only types). A second local backend's pool classification and local-HTML plan then follow its registry `allShortcut` label automatically; the remaining singleton-defuddle assumptions (`url-targets.ts:70,83`, `prepare-html-article.ts:60-91`) fail visibly rather than silently misclassifying when that day comes.

**Wave 13 — Remove the run-level `baseUrl` params from all four write providers.** ADR-005's open question closes as "none": production can never pass the param (`LLMTarget['run']` is 3-ary and `run-llm.ts:111,117` invokes with three args), and endpoint injection keeps one already-typed, already-tested home — the `baseURL` on the config accepted by `runAnthropicCompatibleModel`/`runOpenAICompatibleChatModel`. Drop the 4th param from `runAnthropicModel` (`run-anthropic.ts:12`), `runGrokModel` (`run-grok.ts:25`), `runCerebrasModel` (`run-cerebras.ts:73`), and `runTogetherModel` (`run-together.ts:35`), plus the now-constant `resolveCerebrasBaseUrl`/`resolveTogetherBaseUrl` indirection. Migrate the two tests that inject it — `anthropic-rest-contracts.test.ts:85` and `response-chat-contracts.test.ts:288` — to the default-host `installFetch` assertion pattern the together tests already use (`:326-336`) or to config-level injection. Record the closure in ADR-005; env-var overrides stay deleted (pass 4).

**Benchmarks and fixtures**

**Wave 14 — Fix the whisper benchmark probe; one-model default.** In `isServiceAvailable` (`benchmark-services.ts:27-31`), replace `commandExists('whisper-cpp')` — which can never match, since the project's binary is the managed `runtime/bin/whisper-cli` invoked by absolute path — with an `existsSync` check on that managed path (via `whisperBinaryPath`/`RUNTIME_BIN_DIR` from the runtime-paths utils). In the same change, shrink the whisper `models` array (`benchmark-services.ts:11`) from five entries to `['base']` (~150 MB, meaningful WER; `'tiny'` matches `DEFAULT_WHISPER_MODEL` if minimal download wins), and state in the `--stt-services` help (`define-benchmark-command.ts:100`) that larger sizes are opt-in — the opt-in mechanism (extending `--stt-services` parsing to `service:model` or a dedicated flag) is picked at implementation time. The first fixed run auto-builds whisper.cpp and downloads the model: local and free, but note the disk and wall-clock cost in the benchmark docs.

**Wave 15 — Working `.gitignore` negation chain for `input/`.** The shipped fixture set becomes declarative instead of force-add archaeology. Change line 7 from `input` to `input/*`; add `!input/examples/` and `input/examples/*` followed by five per-subdir unexclusions (`!input/examples/audio/`, `.../batch/`, `.../document/`, `.../lyrics/`, `.../tts/` — .gitignore has no brace expansion); move all file-level `!` entries below the global binary globs (`**/*.png` … `**/*.mp4`, lines 54-59, which currently re-ignore anything the chain allows) and rewrite them to name exactly the 23 tracked files plus the intended extras: `anthony-voice.mp3` (hard-referenced by three test files and `docs/commands.md:117`) and `input/examples/comic/*.md` if it is meant to ship. Delete the stale entries naming paths not in the index (`document/{0-tts-short.txt,1-tts.md,2-urls.md,example-rss-feeds.md,my-dialogue.txt}`). Take care not to over-include personal media sitting in the same directories (`5-audio.mp3`, `synthetic-book.acsm`). Then the maintainer runs a plain `git add input/examples` and checks `git status` shows only intended files; once `anthony-voice.mp3` is tracked, the "committed fixture" claim in `docs/tests/step-4-service-tests-tts.md:27` becomes true and fresh-clone e2e breakage ends.

**Wave 16 — Bless the `stt`/`ocr` tombstone.** Keep the regression guard at `cli-usage-errors.test.ts:311-315` and add a comment naming the policy it encodes: step-2 was unified under `extract`, so `stt`/`ocr` are reserved-and-rejected command names (`tts` is a public command, so these are exactly the names someone would plausibly claim), and deleting this test is the deliberate act required to reintroduce them. No code change.

**Wave 17 — Tighten the weighted-ranking deny-list.** Keep the guard at `combined-report-weighted-ranking-contracts.test.ts:224-234` but match key position (`"<field>":` instead of `"<field>"`) so string values cannot trip it, and add a comment separating the six pure tombstones from `balancedComposite` — a live in-memory field (`combined_report_lib.ts:119`, computed at `:208`) the per-category serializers must keep stripping, which is the entry guarding a real regression path. No builder changes; the guard continues to fire on artifact regeneration, which is the surface users and downstream tooling consume.

**Wave 18 — Port preset coverage to the production path; delete `resolvePresetNames`.** In `prompt-loader-contracts.test.ts`, replace the two `resolvePresetNames` tests (lines 99-117) with a single test looping countrySong/folkSong/jazzSong/popSong and asserting `(await resolveStructuredSchema([name])).presetNames` equals `['standardSongLyrics']`, mirroring the rockSong pattern at lines 190-194 — strictly stronger than today's guard (a lost mapping yields `[]`, and it also fails if the preset is deleted from `preset-registry.ts`, which the current test misses). Drop `resolvePresetNames` from the line-2 import and delete `prompt-loader.ts:315-318`. The redundant creative-writing sibling (lines 112-117) goes in the same change. Coverage strictly improves with no loss window; the cost is four extra schema builds per test run, which is negligible.

## 4. Cleared — do not re-litigate

- **`schemaVersion` optional on `run.json` shapes** — refuted. The finder wanted it required; the skeptic showed the type is correct as-is.
- Everything in the first audit's kept list stays kept: `llama` vs `llama.cpp` naming, the `*-targets.ts` barrels, `fromLegacyCheck`, bare model names in `setup --models`, `--concurrency` runner rejection, `ttsSpeaker` for `--kitten-voice`, `structured-output/compat-fallback.ts`.
- Owner-declined and not re-proposed: the singular `<provider>ImageModel`/`VideoModel`/`MusicModel` fields, and the two-image character identity cards.
- `rolling-shingle-approximation` is live source, not residue.
- The `CLIUsageError` name-string arm: `isCLIUsageError` is `instanceof`-only and returns a type predicate. The class's own `name = 'CLIUsageError'` stays — it is a diagnostics label, not a control-flow key, and renaming it would change every serialized diagnostic payload.
- The five per-provider `-tts` arms in `resolveCheapestModelForFlag`, the local `selectorArgToInternalArgs`, and `decodeLegacyPuaText` and friends: all checked, all live.
- **`downloadFile` still returns a `DownloadResult` no caller reads** — deliberate. The return-type change was scoped out as a signature change, and that call stands.
- ADR-002's module inventory still names the deleted `writeUrlBatchManifest` — kept per the annotate-as-history convention.
- **`.gitignore`'s `runtime/auth/` line** — refuted in the third audit. It looks shadowed by the bare `runtime/` rule, but `docs/cookies.md` documents `runtime/auth/` as the home for browser session credentials (nine references, including `mkdir -p runtime/auth`), and the file already contains negations that pierce `runtime/` for test fixtures. The explicit child rule is defense-in-depth for a documented secrets path, not seed residue.
