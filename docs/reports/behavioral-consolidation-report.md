# Behavioral Consolidation Plan

Date: 2026-08-09. Scope: entire repository on `staging`, refreshed after all 48 mechanical waves landed in `3035df9c`. This plan is the companion to the archival `duplication-report.md` and implements the behavioral-consolidation program as a series of waves and subwaves: Wave 1 fixes every live bug the analysis surfaced (one subwave per bug, small direct diffs), then Waves 2–6 implement the 31 consolidation findings grouped by ascending migration risk (one subwave per finding). Finding IDs (BC-N) are stable and non-contiguous — the program was narrowed from 48 verified findings to 31 in review, and the cut findings' bugs live on as Wave 1 subwaves marked "standalone".

Provenance, condensed: every finding below was produced by one of eleven subsystem analysts (plus a completeness critic) and then re-verified by an independent adversarial verifier that re-read every citation, re-counted every line, diffed allegedly-identical regions, and grepped `test/` for assertions pinning current behavior. All nets are the VERIFIER'S corrected figures (`lines deleted − new engine/adapter/type/import lines`). Full method notes are at the end. Line anchors reference the working tree of 2026-08-09 and WILL drift — re-read every site before extracting, and if two copies differ where this plan calls them identical, surface it as a divergence rather than unifying.

## How to execute a subwave

Every subwave lands `bun run check` clean plus the local test suites named in its text; never run paid or quota-limited e2e suites as verification. Where a subwave names a differential harness (mock-fetch replay, fixture byte-diff, argv corpus), build it BEFORE migrating, not after. The calibration rule inherited from the mechanical report applies throughout: extract-a-new-engine estimates miss, adopt-and-delete estimates beat. Subwaves within a wave are ordered for execution (dependencies noted inline); waves themselves are risk bands, so a later wave may be started before an earlier one finishes except where a dependency note says otherwise.

## Implementation status

W1.4 through W1.19 were completed on 2026-08-09 as direct bug fixes. Their still-open structural consolidation (W6.2) must preserve the behavior recorded below. W3.1 has now structurally absorbed W1.18's contract-lifecycle fix. W3.3 and W3.4 preserved ruling 11's `gemini-stt` identity and strengthened the local STT artifact contract without changing provider execution. W3.5 resolved GLM Reader's finalizer divergence in favor of the shared URL article contract: when the provider omits a final URL, the fetched HTML URL is now backfilled just as it is for the other hosted backends. W3.8 has structurally absorbed W1.7's MiniMax stage fix, W3.9 has structurally absorbed W1.5's Gemini write timeout fix, W3.11 has structurally absorbed W1.14's TTS setup-fee metadata fix, W3.12 has structurally absorbed W1.4's forwarding fix, W4.4 has structurally absorbed W1.6's poll-retry policy, W4.5 has made the image-versus-video media-reference policy forks explicit without changing either domain, W4.6 has converged the synchronous hosted STT request/finalization paths while preserving their transport-specific behavior, W4.7 has structurally absorbed W1.17's Replicate redaction fix while closing the Gemini/Replicate abort-normalization divergence, W4.8 has moved write resume onto the generation single-run engine while preserving its selected-only manifest and message policies, W4.9 has converged actual cost/timing traversal while preserving their recorded valid-run order policies, and W4.10 has deepened the provider-batch resume engine across STT/OCR source, completion, result, and pricing plumbing while preserving their parser-policy forks.

W2.1 through W2.3 were completed on 2026-08-09. The generation resume registry now uses one factory over declarative per-domain configs, the image/TTS defaults contracts use one round-trip harness, and all 16 scoped retry-classifiable HTTP error sites use `httpResponseError`. The implementation removed 335 net lines: 203 from production source and 132 from tests.

Verification after implementation: `bun run check` passed, the local mock Gemini/image/video/MiniMax TTS/music provider contracts passed, the complete local CLI option-resolution contract directory passed, CLI usage-error contracts passed, and the additive resume-provider contract passed. W1.11/W1.12 additionally passed all 34 tests in `ocr-resume-contracts.test.ts`, `all-providers-orchestration.test.ts`, and `manifest-schema-contracts.test.ts`. W1.13/W1.14 passed all 43 tests in `ocr-resume-contracts.test.ts` and `tts-pricing.test.ts`. W1.15-W1.18 passed 103 focused tests across retry classification, image-provider REST, setup download, ElevenLabs clone flow, voice-quality reporting, and HTML URL backends; the two credential-sensitive families were run with inherited provider-key sentinels and passed all 40 tests. W1.19 passed all 9 tests in `chunking-audio-helpers.test.ts`, including the local fake-ffmpeg failure regression. `git diff --check` also passed. No paid or quota-limited provider command was run.

Wave 2 verification: `bun run check` passed. The resume factory and defaults harness passed 35 focused contracts; the HTTP error migration passed 110 local TTS, custom-voice, setup-download, links, image, and video contracts; and the prescribed CLI help, usage-error, and complete option-resolution smoke set passed 189 tests. `git diff --check` passed, and no paid or quota-limited provider command was run.

W3.1 through W3.12 were completed on 2026-08-09. `setupContractSuiteLifecycle` now owns the per-test environment snapshot/clear/restore, fetch restoration, optional `Bun.sleep` restoration, temp-directory cleanup, and suite-specific before/after extensions for 16 lifecycle variants. The canonical `installMockFetch` recorder replaced 65 inline fetch stubs/recorders across 17 files; it also gained `Request`-input normalization so method, headers, JSON/text bodies, multipart forms, and binary sizes are recorded whether callers pass `(url, init)` or a constructed `Request`. Media benchmark's lossy private recorder and its one-use call type were deleted. Native setup's intentionally trivial one-line response stubs and ACSM's response sequencer remain excluded because they are not request recorders. The step-2 model registries now use convention factories over template-literal-typed runtime-key stems, hosted plus local STT e2e assertions now share `assertSttExtractRun`, all five hosted URL article adapters now share `createUrlArticleRun`, and the report fixtures now share `writeMultiProviderRunFixture`. The chat-completion image OCR family now uses one profile-driven runner, and MiniMax TTS, video, and music now depend on a neutral shared protocol client instead of cross-step imports. Write services now share `executeLlmRequest`, lyrics/transcript video rendering now uses a theme-driven ASS builder plus one four-way ffmpeg argv composer, and the preflight plus raw-report step-estimate serializers now use mapped-type field registries instead of synchronized seven-arm switches. The media processing boundary now forwards same-name runtime options through a typed, exhaustive key table. The twelve subwaves removed 917 net lines: W3.1–W3.8 removed 1,058, W3.9/W3.10 added 92 net lines, W3.11 added 195 net lines after contracts, and W3.12 removed 146 production lines.

Wave 3.1/3.2 verification: `bun run check` passed. The migrated REST, image, video, TTS, custom-voice, OCR, STT, URL, setup, benchmark, and report families passed 235 focused contracts across 43 files; the prescribed CLI help, usage-error, and complete option-resolution smoke set passed 189 tests. The credential-sensitive HTML URL and voice-quality families additionally passed all 40 tests with inherited provider-key sentinels, preserving W1.18's snapshot-before-clear guarantee. `git diff --check` passed, and no paid or quota-limited provider command was run.

Wave 3.3/3.4 verification: `bun run check` passed. The pre/post registry projection remained identical, and the provider-selection plus registry-derived configuration families passed all 13 contracts. All 8 local Whisper, Whisperfile, and Reverb e2e cases passed through the shared assertion path; the prescribed CLI help, usage-error, and complete option-resolution smoke set passed 189 tests. `git diff --check` passed, and no paid or quota-limited provider command was run.

Wave 3.5/3.6 verification: `bun run check` passed. The complete local HTML URL backend family passed 22 contracts; all five grouped report categories, all four STT normalization contracts, and all 14 additive-resume contracts passed through the consolidated fixture writers. The prescribed CLI help, usage-error, and complete option-resolution smoke set passed 189 tests. `git diff --check` passed, and no paid or quota-limited provider command was run.

Wave 3.7/3.8 verification: `bun run check` passed. The OpenAI-compatible OCR request-body and page-retry families plus the MiniMax TTS, video, and music provider families passed all 35 focused contracts. The focused additions pin DeepInfra's existing `max_tokens: 4092` and WebP data URL, TTS HTTP status/header preservation through the response-aware decorator, and video's guarded invalid-JSON error with the `video:minimax` stage. The prescribed CLI help, usage-error, and complete option-resolution smoke set passed 189 tests. `git diff --check` passed, and no paid or quota-limited provider command was run.

Wave 3.9/3.10 verification: `bun run check` passed. The OpenAI response/chat, Anthropic REST, Gemini REST, and lyrics-video render families passed all 32 focused contracts. The additions pin direct OpenAI Responses routing through the scaffold, distinct timeout signals for the structured OpenAI-compatible request and its unstructured fallback, byte-identical lyrics/transcript ASS output, and exact ffmpeg argv across the image/spectrogram × ASS/frame-overlay matrix. The prescribed CLI help, usage-error, and complete option-resolution smoke set passed 189 tests, for 221 local contracts in the final combined pass. `git diff --check` passed, and no paid or quota-limited provider command was run.

Wave 3.11 verification: `bun run check` passed. The complete local price-mode family plus reporter pricing-manifest contracts passed all 138 tests, including the recorded seven-step field/order differential for both preflight conversion and raw estimate reporting. The prescribed CLI help, usage-error, and complete option-resolution smoke set passed 189 tests. `git diff --check` passed, and no paid or quota-limited provider command was run.

Wave 3.12 verification: `bun run check` passed. The source/schema differential counted 247 schema keys, 219 direct same-name forwards, 24 derived LLM fields, and four explicit special fields; it found exactly the 16 schema-stripped OCR assignments expected from the old ladder and found no removed live, added, or missing keys. The prescribed CLI help, usage-error, and complete option-resolution smoke set passed 189 tests. `git diff --check` passed, and no paid or quota-limited provider command was run.

W4.1 was completed on 2026-08-09. The image and video quality benchmarks now share one criteria-parameterized vision benchmark engine for strict judge-schema construction, response parsing, score aggregation, provider ranking, provider grouping, quality JSON/Markdown writing, and the three entrypoint log blocks. Image MIME validation and video frame extraction remain domain adapters. The implementation explicitly preserved the pre-migration evidence policy instead of silently choosing one behavior: image provider timing/cost still comes from the first run entry, while video still averages defined values across matching entries. Per-domain report assembly also preserves the legacy `imageFiles`/`imageCount`/`images` and `videoFiles`/`videoCount`/`videos` keys in their original JSON order. The implementation deleted 872 production lines and added 610, for 262 net production lines removed; 41 additive contract lines make the subwave 221 net lines removed overall.

Wave 4.1 verification: `bun run check` passed. `media-benchmark-contracts.test.ts` passed all four local contracts and 81 assertions through mocked judge transport plus local ffmpeg frame extraction; its additions pin top-level/provider JSON key order and the first-entry-versus-average evidence policies. The prescribed CLI help, usage-error, and complete option-resolution smoke set also passed, for 193 tests and 1,661 assertions across 16 files in the combined pass. `git diff --check` passed, and no paid or quota-limited provider command was run.

W4.2 was completed on 2026-08-09. llama.cpp and llamafile now share one completion transport, local-model retry/locking/metadata runner, state-file store, and recorded-server stop engine while retaining their provider-specific startup and model-identity adapters. The stop-policy ruling was resolved by preserving the existing fork explicitly: llama.cpp uses `verified-pid`, which requires unhealthy service state plus PID exit after either signal and clears state only when the recorded PID still matches; llamafile uses `health-clear`, which does not require PID exit and still clears recorded state after the post-SIGKILL health wait regardless of its result. Recovery wiring also remains distinct. The two provider completion clients were deleted and both `run-*` entrypoints are now small typed profiles. The implementation removed 25 net production lines; 132 additive contract lines make the subwave 107 net lines added overall, so the original ~105-line removal estimate missed the cost of representing and pinning the lifecycle-policy fork.

Wave 4.2 verification: `bun run check` passed. The llama.cpp and llamafile local-contract suites passed all 17 tests and 46 assertions, including exact shared completion URL/body/error/signal behavior, both state-file shapes, PID-guarded versus unconditional clearing, the two stop policies, the shared no-process-table health primitive, and llamafile's required `['sh', bundlePath]` launch. The prescribed CLI help, usage-error, and complete option-resolution smoke set passed all 189 tests; the combined pass was 206 tests and 1,626 assertions across 17 files. `git diff --check` passed, and no local-model e2e/download path or paid or quota-limited provider command was run.

W4.3 was completed on 2026-08-09. Image, video, and music pricing now declare one ordered provider table per domain and use the shared `model-selection.ts` helpers for singular/plural normalization, selection presence, provider-specific option construction, target counting, model-option pass-through keys, and estimator priority. The duplicate 9-provider `estimateVideoCost` ladder was deleted; it now returns the first result from the plural estimator. Empty plural arrays explicitly win over singular selectors in every domain. Ruling 10 was resolved in favor of the target data model: video and music cost targets now carry their own duration into their estimator instead of applying the first target's duration to every provider, while a target without a duration still falls back to the domain-wide option. The implementation deleted 601 tracked production lines and added 460 production lines including the new shared helper, for 141 net production lines removed. The 164-line additive contract matrix makes the subwave 23 lines net additive overall.

Wave 4.3 verification: `bun run check` passed. The complete local price-mode family passed all 133 tests and 882 assertions, including a 71-model singular/plural differential across every supported image, video, and music model, three-duration video/music matrices, provider-priority and pass-through-key contracts, empty-plural precedence, single-versus-plural video estimator equivalence, and distinct per-target video/music durations. The prescribed CLI help, usage-error, and complete option-resolution smoke set passed all 189 tests and 1,580 assertions; the combined pass was 322 tests and 2,462 assertions across 27 files. `git diff --check` passed, and no paid or quota-limited provider command was run.

W4.4 was completed on 2026-08-09. The BFL image, Luma Labs image/video, Runway, LTX, Grok, GLM, and MiniMax runners now use one `runPolledJob` lifecycle for single-shot creation, schema validation, retry-classified polling, status reporting, terminal-state handling, and deadline enforcement. GLM retains its Vidu media-field fallback behind the create escape hatch, MiniMax retains its guarded protocol parser behind custom create/poll steps, and BFL retains its server-issued polling URL. The shared file downloader preserves the existing media fork explicitly: image downloads still send the image `Accept` header and reject empty bytes, while video downloads do neither. The duplicated Luma Labs generation schema and provider error-format helpers were also consolidated. The implementation deleted 551 production lines and added 477, for 74 net production lines removed.

Wave 4.4 verification: `bun run check` passed. The complete local image REST and video-provider contract families passed all 33 tests and 152 assertions, covering the eight migrated runners, request URL/body/header/metadata contracts, transient polling and download recovery, MiniMax guarded JSON behavior, and GLM's exactly-three-body Vidu fallback. The prescribed CLI help, usage-error, and complete option-resolution smoke set passed all 189 tests and 1,580 assertions; the combined pass was 222 tests and 1,732 assertions across 25 files. `git diff --check` passed, and no paid or quota-limited provider command was run.

W4.5 was completed on 2026-08-09. Image-generation inputs and video-workflow image/video/audio inputs now use one descriptor-driven media-reference engine for HTTP/data/local classification, MIME and alias resolution, validation traversal, remote byte acquisition, and URL/data/base64 conversion. The domain export surfaces remain unchanged. Ruling 5 was resolved by preserving the existing fork explicitly: image conversion falls back to `image/png` for an unknown local extension and accepts any fetched `image/*` content type, while video-workflow media rejects unknown local MIME and accepts a fetched content type only when it belongs to the selected kind's allow-list. The implementation also preserves image-only download status propagation, video's status omission, image's broad data-URL recognition, video's restricted recognizer, and video normalization of `image/jpg` to `image/jpeg`. The implementation deleted 382 production lines and added 332, for 50 net production lines removed. The 116-line additive differential contract makes the subwave 66 lines net additive overall, so the original ~95-line removal estimate missed the cost of making every policy and user-facing error template declarative.

Wave 4.5 verification: `bun run check` passed. The pre-migration local-file/URL/data-URL differential passed all four tests and 17 assertions before and after adoption, including unknown local MIME, `image/jpg`, unsupported `image/gif`, fetched-content-type fallback, MIME alias normalization, and download error metadata. The complete local provider-selection, image REST, and video-provider families passed all 67 tests and 401 assertions across 17 files. The prescribed CLI help, usage-error, and complete option-resolution smoke set passed all 189 tests and 1,580 assertions; the combined pass was 260 tests and 1,998 assertions across 33 files. `git diff --check` passed, and no paid or quota-limited provider command was run.

W4.6 was completed on 2026-08-09. Deepgram and Grok now use `sttStageRequest` for request timing, request/retry/rate-limit metrics, conservative create retry classification, schema validation, and staged error attachment. The request engine's new failure reader lets Grok retain its parsed error message and raw response, while both providers pass their non-unwrapping attach helpers so retry exhaustion still throws the wrapper rather than its cause. Groq, Together, and DeepInfra now use `finalizeHostedSttResult` through their OpenAI-compatible runner, and Mistral uses the same finalizer while retaining its multipart client, pass controller, and provider-specific 429 cooldown. Ruling 3 was resolved in favor of the shared target contract: all four newly finalized services now emit defined `transcribeMs`, `remoteProcessingMs`, `requestCount`, `retryCount`, and `rateLimitCount` timing fields under the same omission rules as Deepgram and Grok. The OpenAI-compatible adapter preserves its capitalized lifecycle labels through an explicit finalizer option, and Gemini's segment messages now use `logSttSegmentLifecycle` with the existing `gemini-stt` identity. The implementation deleted 229 production lines and added 169, for 60 net production lines removed. The 107-line net additive contract expansion makes the subwave 47 lines net additive overall, so the original ~90-line removal estimate missed the timing-policy and migration-contract cost and also counted finalizer adoption that Deepgram and Grok already had before this subwave.

Wave 4.6 verification: `bun run check` passed. The local OpenAI-compatible, Deepgram, Grok, Mistral, and Gemini REST families passed all 20 tests and 113 assertions through mocked transports. The additions pin OpenAI-compatible request/retry timing counts, Deepgram's staged request plus shared evidence/output path, Grok's parsed raw failure context and non-unwrapped retry-exhaustion wrapper, and Mistral's request/retry/rate-limit timing fields. The prescribed CLI help, usage-error, and complete option-resolution smoke set passed all 189 tests and 1,580 assertions. `git diff --check` passed, and no paid or quota-limited provider command was run.

W4.7 was completed on 2026-08-09. Mistral, Anthropic, OpenAI, Replicate, and Gemini now use one `createProviderRestClient` transport shell for request execution, bounded failure capture, JSON-or-text parsing, error-message extraction, diagnostic attachment, and fetch abort normalization. Typed profiles retain each provider's URL, authorization, default-method, `Accept`, signal, and error-factory policies. OpenAI, Anthropic, and Mistral retain raw-text `.body` plus redacted parsed `.rawResponse`; Gemini retains redacted parsed `.body` without `.rawResponse`; and Replicate retains its `AppProviderError` inheritance, bounded/redacted metadata, stage, retry class, and W1.17 diagnostic policy through the factory-owned escape hatch. Gemini successful JSON responses now use the centralized response reader with its established error text preserved. The shared shell normalizes both DOMException and Error `TimeoutError`/`AbortError` failures to `AbortError`, closing the Gemini/Replicate retry-classification divergence. The implementation deleted 262 production lines and added 217, for 45 net production lines removed. The 169-line additive differential matrix makes the subwave 124 lines net additive overall, so the original ~75-line removal estimate missed the cost of representing the diagnostic-storage forks and pinning all five clients across JSON, text, truncation, and timeout cases.

Wave 4.7 verification: `bun run check` passed. The complete local Mistral, Anthropic, OpenAI REST, Gemini REST, image-provider REST, and provider-client differential families passed all 74 tests and 405 assertions across nine files. The pre-migration differential passed unchanged after adoption for all five error identities, JSON/text messages, status and header propagation, raw-versus-parsed body storage, and 16 MiB bounded-capture truncation; additive cases pin Gemini/Replicate timeout normalization and Gemini's successful-response invalid-JSON message. The prescribed CLI help, usage-error, and complete option-resolution smoke set passed all 189 tests and 1,580 assertions; the combined pass was 263 tests and 1,985 assertions across 24 files. `git diff --check` passed, and no paid or quota-limited provider command was run.

W4.8 was completed on 2026-08-09. `GenerationResumeConfig` now supports additive-stored versus selected-only provider planning, manifest-entry parsing and serialization hooks, deferred input resolution, configurable failure messages, and run/estimate context carrying the selected targets, existing entries, and current manifest. Write declares a selected-only profile over that engine: it parses one-or-many `step3`, loads `prompt.md` only after the no-work decision, serializes one-or-many results, rebuilds write cost/timing through its existing domain hook, and keeps its provider-label-rich incomplete message. Generation domains retain additive stored-provider reconstruction and persistence. Ruling 9 was resolved by preserving write's current manifest contract: write resume does not begin persisting `requestedProviders`. Both write resume failures and the multi-directory dispatch aggregate now use exit-2 `InfraError` values with `resume:generation` and `resume:dispatch` stages. The implementation deleted 308 production lines and added 263, for 45 net production lines removed. The 62-line net contract expansion makes the subwave 17 lines net additive overall, so the original ~70-line removal estimate missed the generic context and regression coverage required by the write adapter.

Wave 4.8 verification: `bun run check` passed. The complete local resume-manifest family passed all 47 tests and 334 assertions, including selected-complete write resume without `prompt.md`, partial-result persistence, exact incomplete text, selected-only omission of `requestedProviders`, staged write failure classification, and staged multi-directory aggregation. The prescribed CLI help, usage-error, and complete option-resolution smoke set passed all 189 tests and 1,580 assertions. `git diff --check` passed, and no paid or quota-limited provider command was run.

W4.9 was completed on 2026-08-09. `walkRunSteps` now owns one typed traversal of `step2` through `step7`, single-or-array normalization, mutually exclusive step-2 discrimination, the TTS character-count gate, and shared Whisper/Reverb model labeling. Cost and timing retain separate typed visitors for their payloads. The existing array-STT-versus-partial-OCR order fork remains explicit through `before-array-stt` and `after-step2` policies, while malformed step-2 objects with neither discriminant and ambiguous objects with both discriminants now skip consistently instead of producing divergent step lists. The two identical OCR pool total-time functions also collapsed into one. The implementation deleted 378 tracked production lines and added 344 production lines including the new walker, for 34 net production lines removed. The 124-line differential contract makes the subwave 90 lines net additive overall, so the original ~70-line removal estimate missed the typed visitor/policy surface and full serialized regression fixture.

Wave 4.9 verification: `bun run check` passed. The complete local price-mode family passed all 135 tests and 888 assertions, including the pre-migration full-run byte differential, the preserved cost/timing ordering fork, shared Whisper/Reverb labels, and consistent malformed/ambiguous step-2 omission. The prescribed CLI help, usage-error, and complete option-resolution smoke set passed all 189 tests and 1,580 assertions. `git diff --check` passed, and no paid or quota-limited provider command was run.

W4.10 was completed on 2026-08-09. `provider-batch-resume.ts` now owns stored output-directory resolution, `file://` source conversion, source-input validation, selected-target completeness evaluation, selected-complete result construction, resume-result projection, and the common STT/OCR price traversal. STT and OCR inject their domain parsers, missing-target builders, metadata readers, and estimate builders. STT retains its `ignoreUnresumableEntries` behavior and strict price parsing; OCR retains its lenient malformed-entry skips, hard source-information failure, and blocked-provider inclusion for explicit selection. URL pricing remains unchanged. The implementation deleted 181 production lines and added 151, for 30 net production lines removed; no test lines were needed because the existing resume contracts already cover the migrated policies.

Wave 4.10 verification: `bun run check` passed. The complete local resume-manifest family, OCR resume contracts, and prescribed CLI help, usage-error, and complete option-resolution smoke set passed all 261 tests and 1,967 assertions across 20 files. A focused pre/post pass over the additive-provider, resume-setup, and OCR resume contracts passed all 54 tests and 152 assertions. `git diff --check` passed, and no paid or quota-limited provider command was run.

## Wave overview

| Wave | Contents | Subwaves | Net LOC | Character |
|---|---|---|---|---|
| W1 | Live-bug fixes, one per bug | 19 | small (mostly additive) | direct behavioral fixes; several later made structural by W2–W6 |
| W2 | Low-risk consolidations — completed 2026-08-09 | 3 | 335 landed | pure wiring/factory/adopt-and-delete |
| W3 | Low-moderate-risk consolidations — completed 2026-08-09 | 12 | 917 landed | adopt-and-delete + profile/factory designs, strong local test cover |
| W4 | Moderate-risk consolidations — completed 2026-08-09 | 10 | 149 net added | engine extractions with differential harnesses |
| W5 | Moderate-high-risk consolidations | 3 | ~480–620 | the largest engine adoptions; paid-adjacent surfaces |
| W6 | High-risk consolidations | 3 | ~280–335 | parse core, resume state machine, paid OCR pipeline convergence |

Waves 2 through 4.10 have landed 1,103 net removed lines, leaving ≈719 estimated net removable lines in W5–W6 after removing W4.10's former estimate and retaining the remaining BC-3+BC-4 overlap deduction. For calibration, the mechanical report's 48 landed waves removed 5,139.

## Rulings required (gate the marked subwaves; do not let a rewrite pick silently)

1. Remote-cleanup policy for async STT jobs (never / unconditional / on-terminal-only differs per provider) and whether the engine's 'cleanup-complete' restamp becomes conditional — gates W1.2 and W5.1.
2. AssemblyAI hand-rolled validation vs real schemas (failure classification stages change) — gates W5.1.
3. Resolved for W4.6: groq, together, deepinfra, and mistral gain the timing fields emitted by the shared hosted STT finalizer. Defined transcribe, remote-processing, request, retry, and rate-limit metrics follow the same omission rules as Deepgram and Grok.
4. Kimi truncation semantics under malformed-page salvage, and deepinfra `max_tokens: 4092` (likely a typo of 4096) — still gates W6.1. W3.7 deliberately preserved both current behaviors in explicit profiles and pinned DeepInfra's value in a request-body contract.
5. Resolved for W4.5: preserve the fork explicitly. Image conversion keeps its silent PNG fallback for unknown local MIME and accepts any fetched `image/*` content type; video-workflow media keeps strict local MIME rejection and fetched allow-list membership.
6. Resolved for W4.2: preserve the fork explicitly. llama uses verified health plus PID exit and PID-guarded state clearing; llamafile uses health-only stop checks and clears state after the post-SIGKILL wait regardless of that wait's result. Their recovery callbacks also remain provider-specific.
7. Comic once-only duplicate-flag errors and `--flag=value` grammar under the native parser — gates W5.2.
8. Resolved for W1.10/W1.11: provider attempts increment once at attempt start; reconciliation preserves that worker-owned count and defaults a success/failure with no state to 1; a scheduler skip preserves the prior count while a provider attempt later classified as skipped counts; and a success preserves `success.relativeDir`, then the stored artifactDir, before using the provider-directory default. Legacy completion fallback evaluates success per requested target: no requested successes is `failed`, every requested target succeeded is `full`, and any partial requested membership is `incomplete`; stale successes from non-requested providers do not count. W6.2 must preserve these landed semantics.
9. Resolved for W4.8: preserve write resume's selected-only manifest contract. Write does not begin persisting `requestedProviders`; TTS, image, video, and music retain additive stored-provider reconstruction and persistence.
10. Resolved for W4.3: video and music cost targets keep their own duration, with the domain-wide duration used only as the fallback for a target that omits one. The former first-target duration flattening was removed.
11. The gemini-stt three-way naming anomaly (suffixed targetService/providerSpecProvider vs OCR's bare gemini) — ruling only; W3.3 must pass it through verbatim either way.
12. Error-kind splits for identical conditions: image ValidationError vs video InfraError for completed-job-missing-URL; zyte ValidationError vs firecrawl/supadata InfraError; Gemini OCR ValidationError vs OpenAI/Anthropic InfraError for blank output; Deepgram TTS InfraError vs siblings' ValidationError (old #12, still open).
13. Standalone: the 58-vs-78-char transcript cue-cap split feeding one fixed one-line layout; the AIMD halving fork (`test/test-runner/adaptive-concurrency.ts:354` halves with `Math.ceil` and drops to 1 on rate-limit where both in-process schedulers use `Math.max(1, Math.floor(cap/2))`); supadata's missing `remoteProcessingMs` timing attribution.
14. Resolved for W1.16: a retry-exhaustion wrapper preserves the final cause's timeout identity. Conservative create classification therefore refuses a wrapped timeout unless `retryAbortOnConservative` is explicitly enabled, exactly as it does for a direct timeout; read-class requests continue to retry it.

## Wave 1 — live-bug fixes (one subwave per bug)

Direct, minimal fixes. Where a later consolidation subsumes the fix structurally, the subwave says so; landing the direct fix first is always acceptable. W1.4 through W1.19 have now landed directly, so their named structural follow-ups must treat the recorded behavior as migration requirements rather than pending fixes.

### W1.1 — HappyScribe STT double-billing window

`run-happyscribe-stt.ts:350` persists a non-resumable 'completed'-stage runtime BEFORE transcript/export retrieval; a crash during export makes the retry re-create a paid order. Fix: move the 'completed' persist after successful transcript/export retrieval so the runtime stays 'polling' (resumable) until the result is in hand. Verify with a mock-fetch replay of the crash-during-export path (the `audio-stt-contracts.test.ts` pattern). Made structural by W5.1, whose engine ordering cannot express the bug.

### W1.2 — Soniox STT deletes remote resources on retryable failures (gated: ruling 1)

`run-soniox-stt.ts:263-268` deletes the remote transcription AND file unconditionally in `finally`, even on a retryable poll-deadline failure while the on-disk checkpoint still says 'polling' — resume then polls a deleted job (404). Fix: gate both deletes on a terminal outcome (finished/failed), matching rev/speechmatics. Subsumed by W5.1's `cleanup.shouldDelete(ctx)` hook.

### W1.3 — OCR rendered-page pool keeps issuing paid requests after a page fails

`page-concurrency.ts` `runOrderedOcrPageTasks` has no early stop: the first failure rejects `Promise.all` while surviving workers keep running — with the preparation cache enabled they keep issuing paid kimi/grok/deepinfra requests for remaining pages against an already-deleted temp dir. Fix: add a stop-scheduling flag checked before each task starts (pipeline B's semantics). Covered by the local mock in `rendered-hosted-pages.test.ts`. The pool is deleted outright by W6.1.

### W1.4 — `bun autoshow write <media>` silently ignores option keys — completed 2026-08-09

The implementation-time type audit corrected the original count from 48 to 47: `directDownload` is present in ProcessingOptionsSchema but not RuntimeOptions, so it is not part of this forwarding boundary. `media-runner.ts` now forwards all 47 previously missing overlapping keys: the happyscribe, supadata, scrapecreators, gemini, together, and whisperfile STT options; the fal image options; the replicate, lumalabs, and fal video options and request controls; the TTS/image/video/music concurrency options; `allVideo`; `promptMd`; and `configPath`. A repeat schema-versus-ladder audit reports no unforwarded RuntimeOptions/ProcessingOptions overlap. W3.12 completed the structural follow-up by replacing the hand-written ladder with a compile-time-complete key table.

### W1.5 — Gemini write requests are unbounded and uncancellable — completed 2026-08-09

`run-gemini.ts` now receives a fresh combined timeout signal from `executeLlmRequest` for each retry attempt and passes it through `geminiGenerateContent`'s `abortSignal`. `gemini-rest-contracts.test.ts` asserts that the write request reaches fetch with an AbortSignal. W3.9 completed the structural follow-up without changing the landed timeout behavior.

### W1.6 — No polled generation runner retries create or poll — completed 2026-08-09

The retry-policy ruling is now explicit: retry idempotent poll reads with `withRetry(runtime_http_read)` and the shared fetch classifier, but do not retry create requests because an ambiguous create failure can duplicate a paid job. The policy is applied to bfl, lumalabs-image, lumalabs-video, runway, ltx, grok-video, glm-video, and minimax-video; the three poll errors that previously omitted HTTP status now attach it so 5xx responses classify correctly. Local mock contracts inject and recover from a transient 503 in every runner. W4.4 completed the structural follow-up by making this policy an invariant of `runPolledJob`.

### W1.7 — MiniMax video/music failures mislabeled as TTS errors — completed 2026-08-09

`ensureMinimaxBaseRespSuccess` and the guarded MiniMax JSON path require a stage, and every TTS, video, and music call site passes its own domain stage. The MiniMax video and music contracts assert `video:minimax` and `music:minimax` on protocol failures while the TTS flow retains `tts:minimax`. W3.8 completed the structural follow-up by moving the protocol module to `src/utils/minimax-client/`.

### W1.8 — `fal` missing from run-video-gen's provider list — completed 2026-08-09

`run-video-gen.ts` now includes `fal[=model]`, and both video and music runtime-path provider-list errors exactly match their command-boundary twins. The chosen punctuation is no trailing period for both domains. The option-resolution contract invokes each empty-target runtime directly and pins the complete strings. Standalone (the command-tail consolidation was cut from the program).

### W1.9 — Video/music usage errors name flags users cannot type — completed 2026-08-09

`retargetUsageErrorsToCommandSpellings` is now a shared command-handler wrapper used by image, video, and music. Video and music export the same option-name maps used to register and normalize their standalone flags, removing the duplicate maps from both command definitions; their command boundaries now translate shared-validator errors to accepted spellings while preserving usage hints and non-usage errors. The MiniMax and Gemini runtime music warnings now name `--duration`, `--lyrics-file`, and `--instrumental` directly. Flag-map contracts cover all three standalone commands, and the CLI usage-error contract exercises the video wrapper end to end. Standalone.

### W1.10 — STT resume manifests never advance attempts; root artifactDir rewritten — completed 2026-08-09

The implementation-time flow audit corrected the attempts diagnosis: `runSttProviderTargetAtIndex` already increments `providerStateMap` once before each provider call, then stores that updated count on success, failure, or a provider-classified skip. Applying OCR's `(existing ?? 0) + 1` again in final reconciliation would therefore double-count failures. `buildProviderStates` now preserves the worker-owned count and defaults a success or failure with no state to 1; missing states preserve their prior count. A scheduler skip made before a provider call preserves zero/the prior count, while a provider call that classifies itself as skipped retains the increment. Successful artifact resolution now uses `success.relativeDir ?? existing.artifactDir ?? providers/<dir>`, preserving resumed root artifacts. The additive resume-provider contract asserts root and provider-directory successes, fresh and already-counted failures, untouched missing state, and both skip paths. Made structural by W6.2, which must preserve this algebra.

### W1.11 — OCR resume can report 'full' with a requested provider missing — completed 2026-08-09

`inferStoredCompletionStatus` now delegates its legacy no-provider-state fallback to the same per-requested-target membership algebra used for state-backed OCR completion, matching STT's immune coding. A contract fixture with one requested success plus two stale non-requested successes proves that equal set cardinality no longer produces `full`; the result is `incomplete`. Made structural by W6.2, which must preserve this fallback.

### W1.12 — URL resume: stale batch.json, skipped sync, success-on-failed-run — completed 2026-08-09

URL batch resume now rebuilds and writes `batch.json` from each current child `run.json` before returning or raising the aggregate exit-2 error. Parent extract synchronization now includes `URL_ARTICLE_ROUTE`; the implementation-time manifest audit corrected the plan's proposed `'url'` literal to the already-persisted `'x-space'` route because `ExtractRoute` and existing manifests do not admit `'url'`. With no backend selected to run, automatic resume now derives `full`/`incomplete`/`failed` from stored completion status instead of forcing success, while an explicit selection that is already complete retains the established selected-provider success behavior shared by STT/OCR. Contracts cover recovery from an all-provider URL failure through child and parent batch persistence, plus a stored `failed` run with no resumable backend producing exit 2. Standalone; its batch-engine adoption was cut from the program.

### W1.13 — OCR checkpoint manifests drop four failure-diagnostic fields — completed 2026-08-09

`buildMetadataErrorEntries` now returns `NonNullable<OcrMetadataOptions['failures']>` directly, with a type guard narrowing `lastError.message` to string. Both checkpoint and final manifest construction consume that single typed result, and both divergent pick-maps were deleted, so `stage`, `status`, `retryAfterMs`, and `rawResponseFile` now survive checkpoint writes alongside the other ten fields. The OCR resume contract constructs one simulated failure carrying all 14 diagnostics, verifies the canonical checkpoint/final value is identical, and pins every serialized field. Standalone.

### W1.14 — preflightToEstimated drops setup-fee metadata for TTS — completed 2026-08-09

The TTS arm of `preflightToEstimated` now conditionally copies `setupCostCents` and `estimateType`, preserving custom-voice setup-fee provenance whenever a preflight estimate exists. The pricing contract pins both fields in the resulting `EstimatedStepEntry`. W3.11 completed the structural follow-up without changing this serialized output.

### W1.15 — Retry-After silently discarded at four attach sites — completed 2026-08-09

All four response-error attach sites now retain the originating `Headers`: ElevenLabs IVC creation, both direct audio/media download paths, and setup's bun-fetch downloader. `AppErrorOptions` and `AppError` gained a typed top-level `headers` property so the setup `InfraError` carries the value without a cast. The ElevenLabs clone-flow and native setup-download contracts pin the returned Retry-After values. W2.3 remains the structural follow-up and must preserve these attachments when it replaces the hand-built errors.

### W1.16 — Retry-After and timeout identity unreachable through retry wrappers — completed 2026-08-09

`withRetry` now hoists final-response headers onto its `retry_exhausted` `AppError` alongside status, while retaining the same metadata copy, so nested hosted-TTS and Mistral retry layers can read Retry-After directly. `classifyFetchRetry` follows nested retry-exhaustion causes for timeout/abort and network identity after applying the wrapper's explicit retryable flag and status; the resolved ruling preserves direct-error semantics, so a conservative create refuses a wrapped timeout unless its override is enabled while an HTTP read retries it. `retry-error-contracts.test.ts` pins the top-level header and 3-second Retry-After decision, and `general-retry-contracts.test.ts` pins both wrapped-timeout classifications. Standalone.

### W1.17 — Replicate REST errors skip redaction — completed 2026-08-09

`ReplicateRestError` now passes parsed provider payloads through `redactPayloadPreview` before storing them in either the top-level `rawResponse` or AppProviderError metadata. It also retains the bounded response's sanitized capture metadata and exposes `bodyBytes`, `bodyTruncated`, and `bodyPreview`, matching the diagnostic shape of the sibling REST clients without retaining an unbounded body. The image-provider REST contract injects secret-shaped API-key and request-id fields and proves they are absent from every stored diagnostic while capture counters remain available. W4.7 structurally absorbed this fix through Replicate's factory-owned diagnostic policy.

### W1.18 — Two contract-test suites run with real API keys present — completed 2026-08-09

Both shared test lifecycles now snapshot their credential environment immediately before each test, clear every in-scope key before the test body runs, and restore that exact per-test snapshot afterward. This replaces the unsafe module-load snapshot while keeping fetch, adapter, bin-dir, and temp-directory cleanup unchanged. The full voice-quality and HTML URL backend families passed 18 and 22 tests respectively when launched with inherited sentinel values for every affected provider key, demonstrating that ambient credentials no longer enter a test. W3.1 remains the structural follow-up and must preserve the snapshot-clear-restore ordering.

### W1.19 — Concat list file leaks on ffmpeg failure for nine TTS runners — completed 2026-08-09

`concatAndConvertToWav` now encloses concat execution and result validation in a `try` and removes `speech-<provider>-chunks.txt` from its `finally`, covering successful concatenation, a nonzero ffmpeg exit, and an execution-time throw for every caller. The focused TTS helper contract installs a failing local fake ffmpeg, pins the existing `InfraError` message, and proves the concat list is absent after the rejection. Standalone.

## Wave 2 — low-risk consolidations

### W2.1 — BC-22: `buildGenerationResumeHandler` — registration factory for the four generation resume domains — completed 2026-08-09 (net 149)

`GenerationResumeConfig` now owns `modelFields`, `buildEstimates`, and `priceAggregateOptions`; `generation-resume.ts` derives provider-target reconstruction and aggregate pricing from those fields and exports `buildGenerationResumeHandler`. TTS preserves its asynchronous estimates plus `ttsTimingCharacterCount`/`ttsInputText`, and music still exports `finalizeMusicResumeArtifacts`. The four domain files are now flags, model-field tables, run/rebuild hooks, and typed configs; the registry's four hand-written handler stanzas are factory calls. The surface contract now exercises `hasResumableWork` through the registered factory output, while the additive contract directly pins reconstruction and pricing. The subwave removed 149 net lines.

### W2.2 — BC-45: image-tts-defaults round-trip harness — completed 2026-08-09 (net 143)

`image-tts-defaults.test.ts` now uses the file-local `expectConfigPatchRoundTrip(flags, domain, expectedValues, { merge })` harness. It derives the explicit flag set and merge expectation from the input record, making record/Set and record/merge drift unrepresentable. The hosted-image case explicitly passes `merge: false`, preserving its prior coverage boundary instead of silently adding a new assertion. All seven original test cases pass unchanged, and the subwave removed 143 net lines.

### W2.3 — BC-35: `httpResponseError` — one constructor for retry-classifiable HTTP failures, 16 sites — completed 2026-08-09 (net 43; subsumes dropped TT-4 + X-6's error slice + old divergence #8)

`rest-client.ts` now exports `httpResponseError(message, response, extras?)`, which constructs an `Error` with canonical `status` and `headers` attachments plus optional retry metadata. All 16 scoped sites migrated: five TTS runners, Speechify custom voices, shared TTS HTTP, Hugging Face and links downloads, image/video download builders, ElevenLabs IVC, both direct audio/media downloads, setup's bun-fetch downloader, and GLM OCR. The unused `VideoOutputDownloadHttpError` class and name disappeared. The MiniMax ruling is to preserve its `InfraError` base; its small wrapper now passes `stage`, `status`, and `headers` through typed `InfraError` options. Payload-bearing provider error factories remain excluded. Exact TTS messages, W1.15 Retry-After propagation, image/video retry classification, and setup download behavior passed their local contracts. The subwave removed 43 net lines.

## Wave 3 — low-moderate-risk consolidations

### W3.1 — BC-40: `setupContractSuiteLifecycle` — one env/fetch/tempdir lifecycle for contract-suite variants — completed 2026-08-09 (combined W3.1/W3.2 net 714; generalizes wave 3's TE-1)

`rest-contract-helpers.ts` now exports `setupContractSuiteLifecycle({envKeys, tempPrefix, restoreBunSleep?, beforeEachExtra?, afterEachExtra?})`. It snapshots fetch and every named environment key immediately before each test, clears the keys, optionally snapshots `Bun.sleep`, runs the suite extension, then restores all state and removes tracked temp directories even when suite-specific cleanup fails. Sixteen lifecycle variants adopted it, including the REST provider families, generation shared suites, TTS, async STT, ScrapeCreators, ElevenLabs clone flow, native setup, TTS dialogue, HTML URL, voice-quality, and media-benchmark contracts. Fal uses `beforeEachExtra` to install its fixture key after clearing; HTML URL uses `afterEachExtra` to restore adapter and bin-dir state. OpenAI/video shared re-export consumers were audited before obsolete mutable lifecycle exports were removed. The two W1.18 suites still take their environment snapshot immediately before clearing and restore that exact snapshot; inherited-sentinel verification passed all 40 contracts.

### W3.2 — BC-41: adopt `installMockFetch` at the remaining inline fetch-recorder sites — completed 2026-08-09 (combined W3.1/W3.2 net 714; extends wave 32's TE-4). After W3.1.

Sixty-five inline fetch stubs/recorders across 17 files now route through `installMockFetch`. Provider assertions consume its canonical URL, method, normalized `Headers`, `bodyJson`, `form`, and binary-size fields instead of maintaining site-specific URL-only, authorization-only, raw-header, or `JSON.parse(String(body))` call shapes. The helper now also normalizes a constructed `Request`, including its method, headers, JSON/text body, multipart form, or binary byte length; this preserves Mistral saved-voice coverage that previously needed a private Request-aware recorder. Media benchmark's lossy `installFetch` clone and `MediaBenchmarkFetchCall` type were removed. Native setup's trivial response-only stubs and ACSM's ordered response sequencer remain direct because they record no request contract. The combined W3.1/W3.2 diff removed 1,297 lines and added 583, for 714 net removed lines, substantially beating the overlap-adjusted estimate.

### W3.3 — BC-38: convention factories for the step-2 provider registries — completed 2026-08-09 (net 163)

`entry-builders.ts` now exports `sttModelProvider(slug, keyStem, options)` and `ocrModelProvider(slug, keyStem, options)`. Both derive the step, modality, flag, config path, bootstrap identity, shortcuts, and singular/plural runtime keys; the key stem is constrained to paired `RuntimeOptions` keys with template-literal types, so callers cannot pair one provider's plural key with a neighboring singular key. All 8 OCR model providers and all 17 STT model providers adopted the factories; the genuinely boolean Tesseract and Reverb entries remain direct. Labels and supported-model registries remain explicit. The observable registry order is unchanged, with Whisper and Whisperfile last. ScrapeCreators still opts out of `all-stt`, local Whisper providers retain their unsuffixed bootstrap IDs and `all-local-stt`, and Gemini explicitly passes through `targetService` and `providerSpecProvider` as `gemini-stt`. A serialized pre/post projection of both complete registries deep-compared equal. The subwave deleted 270 lines and added 107, for 163 net removed lines.

### W3.4 — BC-42: `assertSttExtractRun` — divergence-explicit assertion harness for local STT e2e — completed 2026-08-09 (net 68)

`assertSttExtractRun` now owns the shared transcript, provider-result, evidence/raw absence, prompt, text-summary absence, run-manifest, requested-provider, provider-state, missing-provider, and optional split-segments assertions. Its spec names the real divergences: transcript matcher, requested target and origin, exact-vs-contained runtime model, prompt expectation, resolved-step/provider-state coverage, and split directory. The hosted `defineSTTServiceTest` tail and all 8 cases across the four local Whisper, Whisperfile, and Reverb files use that one path. This restores the prompt check to both split cases, the text-summary absence check to large-v3-turbo split, the complete artifact and manifest stack to Reverb, and the `result.json` plus evidence/raw-absence checks to every local engine. Direct `budgetedTest` literals are unchanged. The Whisper-local and shared test-helper ANSI regex copies were replaced with the canonical `stripAnsi`. The subwave deleted 248 lines and added 180, for 68 net removed lines.

### W3.5 — BC-23: `createUrlArticleRun` adapter factory for the URL article provider family — completed 2026-08-09 (net 58; mechanical U-2 already landed)

`createUrlArticleRun` now owns the shared log → provider scrape → `finalizeUrlArticleResult` wrapper for Firecrawl, GLM Reader, Spider, Supadata, and Zyte. Each provider keeps its request builder and response parser, the generated run function retains the explicit optional base-URL fourth parameter, and the registry still stores an assignable `run` property for the retry and orchestration contracts to stub. GLM Reader migrated last: its internal `preparedMarkdown` result is now the family-standard `markdown`, and the implementation-time behavior ruling enables the shared final-URL backfill while preserving its request, source-URL override, title, validation, and file-size behavior. `pickCleanString` replaced the Firecrawl/Zyte single-record getter twins; Spider retains its two-record per-key precedence. `ensureMeaningfulMarkdown` now uses one backend display-name table after Defuddle's intentionally distinct remediation message. The WebArticleMetadata assignment ladders remain provider-local. The subwave deleted 131 production lines and added 74, while the GLM contract removed one net test line, for 58 net removed lines.

### W3.6 — BC-43: `writeMultiProviderRunFixture` — one builder for hand-assembled run.json + providers/* fixture trees — completed 2026-08-09 (net 49; subsumes dropped TE-7)

The implementation audit corrected the original “six suites” scope to four actual multi-provider run/provider fixture-tree writers: the grouped OCR, STT, and URL reports plus STT normalization. `writeMultiProviderRunFixture` now lives beside the neutral manifest helpers and derives full service/model/artifactDir/status provider states, actual cost steps, actual timing steps, and every schema-versioned provider result from one providers array. OCR and STT consensus readers passed unchanged with the fuller provider-state shape, eliminating the sibling fixtures' incompatible beliefs. The existing manifest writers now supply both envelopes, with `writeRunManifestFixture` admitting the test-only `url` report kind. Resume-additive's compacted STT result also adopted `writeProviderResultFixture`; this deliberately changes that ephemeral fixture from compact to pretty JSON while remaining parse-equivalent. The subwave deleted 167 test lines and added 118, for 49 net removed lines.

### W3.7 — BC-4: profile-driven chat-completion image-OCR runner for kimi/grok/deepinfra — completed 2026-08-09 (51 production lines removed; net 31 after contracts)

`createChatImageOcrRunner` now owns image-size validation, MIME/data-URL construction, page retry wiring, chat dispatch, usage extraction, page result construction, and the hosted-document adapter. Kimi, Grok, and DeepInfra are declarative profiles for service/method identity, temp prefix, image limits, MIME support, prompt, client config, error prefix, and request body; Kimi alone supplies the response hook that converts `length`/`max_tokens` finish reasons into `OcrStructuredResponseError(provider_limit)`. Ruling 4 remains open for W6.1, so this subwave preserved Kimi's truncation throw, Grok/DeepInfra's pass-through behavior, Grok's `max_completion_tokens: 4096`, and DeepInfra's existing `max_tokens: 4092` rather than silently normalizing any of them. The Grok/DeepInfra request contract now pins the token-field split and DeepInfra WebP support; the existing Kimi request and retry-exhaustion contracts pass through the shared runner unchanged.

### W3.8 — BC-9: `src/utils/minimax-client/` — one MiniMax protocol module for TTS, video, music — completed 2026-08-09 (10 production lines removed; 25 net lines added after contracts; subsumes dropped X-6 and W1.7)

The TTS-owned `minimax-utils.ts` is gone, eliminating the video→TTS and music→TTS imports. `src/utils/minimax-client/minimax-client.ts` now owns the base-response validator, create/query schema superset (including nested `error_message`), guarded JSON parser, authenticated JSON request builder, `minimaxFetchJson`, task status/file-id helpers, and `retrieveMinimaxFileUrl`. Music and TTS use the shared fetch/validation shell; TTS passes `withHostedTtsRetry` through the execution hook so only the HTTP request/response check is retried, and its response-aware `decorateError` preserves `.status` and `.headers` for retry classification without retrying deterministic parse or business failures. Video consumes the same create/query schemas, parser, status/file helpers, and retrieve helper; W4.4 subsequently moved its W1.6 poll-only retry loop into `runPolledJob` without changing the MiniMax parser. Invalid video JSON now produces the same staged `ValidationError` shape as TTS/music instead of a raw `SyntaxError`, and TTS can now surface a nested query `error_message`. The estimate missed the cost of the generic schema/retry hooks: production still removed 10 lines, while 35 additive contract lines made this subwave net-additive; combined W3.7/W3.8 remain 6 net removed.

### W3.9 — BC-14: `executeLlmRequest` — finish the write-service request framework — completed 2026-08-09 (41 production lines removed; net 1 removed after contracts; subsumes W1.5)

`write-utils/llm-request-scaffold.ts` now owns the conservative create retry loop, optional policy override, empty-text validation, instrumentation, step-3 metadata construction, error logging, and 30-minute timeout composition for both compatibility engines and the standalone OpenAI, Gemini, and MiniMax runners. Provider configuration is prepared inside the scaffold's logged boundary but before retry classification, so missing credentials keep their failure log without being treated as transient requests. The execute closure receives a signal factory; OpenAI-compatible structured fallback calls it separately for the schema request and the response-format-free request, preserving a fresh cap for each. Structured-output shaping, each provider's classifier, MiniMax `base_resp` validation, and provider response/usage extraction remain local closures. The seven thin OpenAI/Anthropic-compatible adopters now pass config factories so their missing-key failures also reach the shared log boundary. The response-chat contract added direct OpenAI Responses coverage and proves the two structured-fallback signals are distinct. The estimate missed the scaffold's typed prepare/execute boundary and the additive differential coverage: production removed 41 lines, while 40 net contract lines made the subwave net 1 removed.

### W3.10 — BC-34: render.ts internals — ASS theme model + decide-then-compose ffmpeg args — completed 2026-08-09 (9 production lines added; 93 net lines added after contracts; verdict CONFIRMED as filed)

`LYRICS_ASS_THEME` and `TRANSCRIPT_ASS_THEME` now feed one `buildCueAss`; `buildAss` and `buildTranscriptAss` remain compatibility wrappers. The behavior ruling is conservative and explicit: transcript's fixed 2/1/1/1 shadows, unused `TranscriptSpeaker` style, and both renderers' existing layer orders remain theme data so the generated documents stay byte-identical. The cue fixture contract pins both complete ASS outputs by SHA-256. `renderLyricsVideo` now decides between ASS and generated-frame overlays first, then passes that tagged source plus the background choice to `buildLyricsVideoFfmpegArgs`. The composer owns input ordering, filter composition, audio-stream indices, encoder settings, upright-video metadata, audio encoding, and the shared tail across all four modes; the `__fallback__` sentinel and four hand-maintained arrays are gone. The argv contract deep-compares the image/spectrogram × ASS/frame-overlay matrix, providing the fallback coverage that was previously absent. The estimate missed the declarative theme and differential-harness cost: production added 9 lines and the 84-line net contract addition made the subwave 93 lines net additive, though W3.9/W3.10 together still removed 32 production lines.

### W3.11 — BC-31: preflightToEstimated as a mapped-type field registry — completed 2026-08-09 (41 production lines removed; 195 net lines added after contracts; subsumes W1.14)

`step-estimate-fields.ts` now owns `STEP_FIELDS`, whose mapped type validates each field list against the corresponding `StepEstimate` member, and the exact-optional-safe `pickDefined` copier used by `preflightToEstimated`. The registry order reproduces every previous `EstimatedStepEntry` key exactly, including W1.14's `setupCostCents` and `estimateType`; `setupTimeMs`, notes, and the other deliberately excluded fields remain absent. `STEP_REPORT_FIELDS` also absorbed reporter's last synchronized seven-arm switch while preserving its raw-result aliases, field order, omissions, Whisper/Reverb display identities, and trailing `totalCostCents`. The implementation deleted 211 production lines and added 170, for 41 net production lines removed. The recorded all-step differential fixture pins both serialized projections byte-for-byte, but its 236 additive contract lines make the subwave 195 lines net additive.

### W3.12 — BC-24: typed key-table + completeness gate for processMediaSingle's forwarding ladder — completed 2026-08-09 (net 146; subsumes W1.4)

`media-runner.ts` now declares 219 direct same-name forwards in `PROCESSING_FORWARD_KEYS`, with `satisfies readonly (keyof RuntimeOptions & keyof ProcessingOptions)[]` rejecting keys outside either side of the boundary. `MissingProcessingForwardKey = Exclude<keyof ProcessingOptions, ProcessingForwardKey | ProcessingSpecialKey>` drives the key copier's parameter type, so any newly unhandled schema field makes the table fail compilation. The special-key set explicitly accounts for `url`, `filePath`, `outputDir`, schema-only `directDownload`, and the 24 LLM fields that still come from `buildLLMModelOptions(resolveLLMDefaults(...))`. The migration retained every live same-name forward, including all 47 W1.4 additions, and deleted the 16 OCR assignments that `ProcessingOptionsSchema` always stripped. The implementation deleted 236 production lines and added 90, for 146 net lines removed.

## Wave 4 — moderate-risk consolidations

### W4.1 — BC-30: one criteria-parameterized vision-judge benchmark engine for image + video — completed 2026-08-09 (net 221; subsumes backlog B-6)

`vision-benchmark-engine.ts` now owns the criteria-derived strict JSON schema, judge transport and validation, score/evidence aggregation, provider ranking, first-or-average provider-stat resolution, common quality-report base and Markdown/JSON writers, and the shared benchmark input/report/ranking log sequence. The image and video modules supply typed criterion descriptors with report labels, prompt lines, and Markdown columns plus their domain-specific artifact preparation and report assembly; image retains local MIME validation and data URLs, while video retains ffmpeg/ffprobe validation, frame extraction, duration evidence, and ordered-frame prompts. The former video judge, provider, and reporting modules were deleted, and the constants module now contains only the shared frame count needed by frame extraction.

The migration resolved the evidence-semantics fork by preserving both established behaviors through an explicit resolver policy: image uses `statsPolicy: 'first'`, and video uses `statsPolicy: 'average'`. Domain adapters assemble the serialized fields in legacy order, including the distinct `imageFiles`/`imageCount`/`images` and `videoFiles`/`videoCount`/`videos` keys. The local mocked-judge contract now pins those key orders and directly exercises both stats policies. The implementation deleted 872 production lines and added 610, for 262 net production lines removed; 41 additive contract lines make the landed total 221 net lines removed, essentially matching the verifier's ~220 estimate. A future third media quality domain can now provide descriptors and artifact/report callbacks instead of copying either pipeline.

### W4.2 — BC-13: finish the local-LLM server framework for llama.cpp and llamafile — completed 2026-08-09 (25 production lines removed; 107 net lines added after contracts; mechanical W-1/W-2 already landed)

`local-completion-client.ts` now owns the formerly line-for-line chat-completion request, strict response validation, empty-response retries, provider error text/stages, and token-count fallback. `local-model-runner.ts` owns the process lock, retry/recovery shell, timeout propagation, request timing, and identical `Step3Metadata` assembly. `run-llama.ts` and `run-llamafile.ts` are typed profiles supplying the base URL, service/stage, process-lock name, model resolver, and provider-specific recovery callback; the two provider client files and their duplicated constants were deleted.

`local-server-state.ts` now owns state paths plus read/write/clear mechanics while provider adapters retain the llama `{ pid }` shape and llamafile's `{ pid, port, model, createdAt }` shape. `local-server-stop.ts` adopts the existing health/PID/wait primitives rather than duplicating them and makes the resolved lifecycle fork declarative: llama's `verified-pid` policy requires both health loss and process exit after SIGTERM or SIGKILL and uses PID-guarded state deletion, while llamafile's `health-clear` policy keeps its health-only SIGTERM check and unconditional state clear after the post-SIGKILL health wait. `stopDefaultLlamaServer` still preserves a healthy unrecorded service, `stopLlamafileServerForRecovery` still targets only recorded state, and both public stop functions retain the test helpers' `{ lockRoot }` resource option.

The source-text contracts for `process.kill(pid, 0)` now point at the shared health primitive, while the llamafile provider contract continues to pin `['sh', bundlePath]` at the provider-specific APE launch site. Additive contracts also pin both completion profiles, AbortSignal forwarding, provider error classification, state round trips, guarded versus unconditional clearing, and the two explicit stop policies. The implementation deleted 426 tracked production lines and added 87, then added 314 shared/profile production lines, for 25 net production lines removed. The 132-line net contract addition makes the subwave 107 lines net additive overall; the original estimate did not budget the explicit policy adapters or migration contracts needed to preserve ruling 6 without hiding the divergence.

### W4.3 — BC-28: per-domain provider model-selection tables for the pricing pipelines — completed 2026-08-09 (141 production lines removed; 23 net lines added after contracts)

`model-selection.ts` now owns `collectSelections`, `optionsForService`, `hasAnySelection`, and `passThroughKeys` over exact-optional-safe provider specs. `IMAGE_PRICING_PROVIDERS`, `VIDEO_PRICING_PROVIDERS`, and `MUSIC_PRICING_PROVIDERS` declare provider order plus the plural and singular runtime keys next to their estimators. Those tables now drive estimator traversal, aggregate price-mode presence and option forwarding, standalone-video preflight option construction, estimated-cost target routing, video timing routing, generation target counts, domain option builders, and the media-runner processing pass-through list. Adding a provider still requires its domain-specific estimate branch, but there is no second membership/key enumeration to update across the pricing pipeline.

The shared normalizer deliberately implements plural-wins semantics whenever the plural property is defined, including an empty array. `estimateVideoCost` now delegates to `estimateVideoCosts(options)[0]`, so singular and plural entrypoints share provider priority, validation, fallback, and normalization. Ruling 10 was resolved by estimating each video or music target separately in table priority order and passing `target.durationSeconds ?? input.videoDuration` or `target.durationSeconds ?? input.musicDuration`; this removes the cross-provider first-duration leak without changing domain defaults or provider-specific duration normalization. The contract matrix compares singular and plural estimates for all 71 supported models, runs video and music models across three durations, and separately pins ordered tables, derived key counts, empty-plural precedence, single-video delegation, and distinct target durations. The implementation deleted 601 tracked production lines and added 460 production lines including the 73-line helper, for 141 net production lines removed; 164 additive contract lines make the landed total 23 lines net additive overall.

### W4.4 — BC-8: `runPolledJob` — one submit→poll→download engine for the eight hand-rolled polled generation runners — completed 2026-08-09 (74 production lines removed; preserves W1.6 as engine policy)

`src/utils/polled-job-client/` now owns `runPolledJob<TCreate, TPoll>`, which executes a single non-retried create step, validates direct HTTP responses through the supplied Valibot schema, runs polling through `pollUntil` plus `withRetry(runtime_http_read)`, emits provider status through a hook, and applies provider-specific success/failure predicates. BFL image, Luma Labs image/video, Runway, LTX, Grok, GLM, and MiniMax all use the engine. The direct HTTP profile preserves raw-text video errors and the image adapters retain JSON-or-text parsing through `extractImageErrorMessage`. GLM's custom create step preserves the Vidu fallback's three ordered bodies without treating them as generic retries, while MiniMax's custom create/poll steps preserve its response-aware guarded parser and `video:minimax` error stage.

The engine keeps the existing provider policies rather than silently normalizing them: create remains single-shot under W1.6, poll failures carry HTTP status into the shared retry classifier, BFL uses its server-issued polling URL, and the existing 5-second versus 10-second intervals remain unchanged. `downloadGeneratedFile` now backs both output paths but retains image-only `Accept` negotiation and empty-byte rejection; video downloads still send no `Accept` header and retain their prior byte policy. The duplicate Luma Labs generation schemas now have one home, the equivalent Runway/LTX/Grok/GLM error formatters use one helper with an opt-in nested-message rule for LTX, and Gemini video remains on its distinct Gemini REST transport. The implementation deleted 551 production lines and added 477, for 74 net production lines removed; the ~90–115 estimate missed the explicit direct-HTTP/custom-step boundary needed to keep GLM and MiniMax behavior visible.

### W4.5 — BC-10: one kind-descriptor media-reference engine for image-inputs / video-media-inputs — completed 2026-08-09 (50 production lines removed; 66 net lines added after contracts)

`media-reference-engine.ts` now owns HTTP/data/local classification, extension and alias lookup, validation traversal, remote fetches, local/data byte resolution, and URL/data/base64 conversion. `MediaKindSpec` makes allowed MIME types, extension maps, aliases, data-URL recognition, data MIME enforcement, unknown-local-MIME behavior, fetched-content-type policy, fallback MIME, `Accept` headers, default file names, error templates, stages, and status propagation declarative. `image-inputs.ts` remains the image-provider adapter with its provider/model-specific allow-lists and mask validation; `video-media-inputs.ts` declares the image/video/audio descriptor table and retains the local ffprobe helpers. All existing domain-only exports remain in place.

Ruling 5 preserves both established policy forks instead of silently choosing one global behavior. Image references use `fallback → image/png` for unknown local extensions and accept any fetched `image/*` response, while video-workflow references throw for unknown or wrong-kind local MIME and require fetched content-type membership in the selected kind's allow-list, with URL-extension fallback when the response type is unusable. The engine also preserves the less obvious divergences: image validation rejects `data:image/jpg` while its converter passes it through; video normalizes it to `image/jpeg`; image recognizes unsupported `data:image/gif` as a data URL while video retains its existing missing-file classification; and only the image download error carries HTTP status. The offline differential matrix was added before migration and passed unchanged after both adapters adopted the engine. The implementation deleted 382 production lines and added 332, for 50 net production lines removed; the 116-line contract makes the landed total 66 lines net additive. The estimate missed the descriptor and differential cost required to keep the policy forks and exact user-facing failures observable.

### W4.6 — BC-2: route the synchronous hosted STT runners through `sttStageRequest` + `finalizeHostedSttResult` — completed 2026-08-09 (60 production lines removed; 47 net lines added after contracts)

`sttStageRequest` now accepts a typed failure reader returning both the display message and raw response. Deepgram supplies its exact plain-text failure policy, while Grok supplies its guarded JSON parser so nested provider messages and parsed response context survive the shared shell. Both adapters retain and pass their non-unwrapping attach helpers; a retry followed by a terminal failure therefore still throws `withRetry`'s `retry_exhausted` wrapper with the provider failure as its cause. The engine owns request counting, retry/rate-limit callbacks, conservative create retry policy, timeout signals, HTTP error construction, response JSON parsing, schema validation, and validation context for both providers.

`openai-compatible-single-speaker.ts` now records request, retry, rate-limit, and transcribe metrics around its existing OpenAI REST transport, then routes Groq, Together, and DeepInfra through `finalizeHostedSttResult`. A `lifecycleProvider` option keeps their existing capitalized segment-log labels while metadata continues to use the normalized service names. Mistral also adopts the finalizer but retains its provider-specific multipart transport, serialized pass controller, Retry-After/fallback cooldown, and retry-wrapper unwrapping policy. Its classifier now records the same retry and rate-limit counts as the other synchronous hosted runners. The shared finalizer continues to own transcript fallback, file output, token counts, evidence, completion logging, and timing-field omission.

Ruling 3 was resolved in favor of the shared target data model: Groq, Together, DeepInfra, and Mistral now gain defined `transcribeMs`, `remoteProcessingMs`, `requestCount`, `retryCount`, and `rateLimitCount` entries in `metadata.timings`; zero-valued fields remain omitted. Gemini's two raw segment messages were replaced by `logSttSegmentLifecycle` using its existing `gemini-stt` service identity. The migration deleted 229 production lines and added 169, for 60 net production lines removed. Three additive contracts expanded the test surface by 107 net lines and make the subwave 47 lines net additive overall; the original ~90-line removal estimate counted finalizer work that Deepgram and Grok already shared before this migration and did not budget the explicit timing policy or regression coverage.

### W4.7 — BC-36: `createProviderRestClient` — one transport shell for the five provider REST clients — completed 2026-08-09 (45 production lines removed; 124 net lines added after contracts; subsumes W1.17; builds on W2.3)

`createProviderRestClient` now owns fetch execution, bounded error-response capture, shared JSON-or-text parsing and message extraction, capture metadata, status/header/body diagnostics, and `normalizeFetchAbortError`. Each client supplies a typed request builder, error-message prefix, and error factory. Mistral retains its `/v1` normalization, bearer auth, JSON `Accept` default, POST default, and timeout-derived signal; Anthropic retains its API version/beta headers, JSON `Accept` default, and POST/DELETE selection; OpenAI retains its bearer-only header policy and POST default; Replicate retains its explicit JSON `Accept`, caller-selected method, stage, and retry class; and Gemini retains its API-key headers and caller-required GET/POST/DELETE methods. Gemini's duplicated `parseJsonOrText` was deleted, and successful Gemini JSON requests now use `readJsonResponse` with a message-prefix option that preserves both established validation messages.

The error-factory boundary preserves the verifier's class and storage constraints. Mistral and Anthropic still throw ordinary `Error` instances; OpenAI still throws `OpenAIRestError`; Gemini still throws `GeminiRestError`; and Replicate still throws `ReplicateRestError extends AppProviderError`. The default diagnostic policy stores raw text in `.body` and a redacted parsed payload in `.rawResponse`; Gemini selects parsed/redacted `.body` with no `.rawResponse`; Replicate selects factory-owned diagnostics so its W1.17 redaction, bounded metadata, body counters/preview, stage, retry class, and `AppProviderError` metadata remain intact. Provider URL, authorization, `Accept`, method, and signal differences remain visible in their profiles, while the former Gemini/Replicate transport gap is closed: fetch `TimeoutError` and `AbortError` values are normalized to an `AbortError` before retry classification just like the other three clients. Cheap response-attachment sites continue to use `httpResponseError` unchanged.

The offline differential matrix was added and passed before migration, then passed unchanged after all five profiles adopted the engine. It covers JSON and plain-text errors, multiple statuses, retry headers, exact error identity and `AppError` membership, raw-versus-parsed body storage, and oversized responses beyond the 16 MiB capture ceiling for every client. Additive timeout cases prove Gemini exposes a normalized `AbortError` and Replicate retries it conservatively before preserving the normalized cause in its exhaustion wrapper; a successful-response invalid-JSON case pins Gemini's legacy message. The implementation deleted 262 production lines and added 217, for 45 net production lines removed. The 169-line contract makes the landed total 124 lines net additive; the estimate did not budget the declarative diagnostic forks or the five-client truncation matrix.

### W4.8 — BC-19: write-resume adopts the generation-resume single-run engine — completed 2026-08-09 (45 production lines removed; 17 net lines added after contracts; subsumes backlog R-4; fixes old divergence #9)

`GenerationResumeConfig` now exposes `parseManifestEntries`, deferred `resolveInput`, `selectionMode: 'additive-stored' | 'selected-only'`, `serializeEntries`, and `failureMessage`. Its run and estimate callbacks also receive the selected targets, parsed existing entries, and current manifest metadata, which lets the shared lifecycle own read/validate, no-work classification, execution logging, merge/still-missing handling, rebuild, write, pricing, and exit-2 failure classification without hiding write-specific preparation. The generation configs explicitly select `additive-stored`; their prior input/requested-provider validation, target reconstruction, provider persistence, exact no-work strings, array metadata, and concise incomplete count remain unchanged.

`writeResumeConfig` selects `selected-only`, parses the existing one-or-many `step3` representation, collects targets only from explicit write provider flags, loads `prompt.md` after the engine has ruled out no-work, and serializes merged entries back through `serializeOneOrMany`. Its run adapter retains structured-schema resolution, collision-free output naming, show-note artifacts, optional rendered text, and the existing cost/timing rebuild. The no-selection, selected-complete, processing, and provider-label-rich incomplete strings remain exact; the incomplete-richness decision therefore preserves write's existing labels rather than flattening it to the other generation domains' count-only text. Ruling 9 also preserves the manifest fork: write does not add `requestedProviders`, while the four additive generation domains still do.

The old write resume read/validate, run/merge/write, and price lifecycles were deleted in favor of thin exported delegates plus a factory-built registry handler. Bare write failure errors now become `InfraError` with stage `resume:generation` and exit code 2, and the multi-directory aggregate ride-along uses `resume:dispatch` with the same exit code. Additive contracts pin the deferred-input ordering with a completed write run lacking `prompt.md`, selected-only manifest persistence, partial result storage, exact incomplete text, and both new stages. The implementation deleted 308 production lines and added 263, for 45 net production lines removed; 62 net additive contract lines make the landed total 17 lines net additive. The estimate did not budget the typed run/estimate context or the regression coverage needed to make write's policies explicit.

### W4.9 — BC-29: `walkRunSteps` — one visitor over the run structure for computeActualCosts / computeActualProcessingTimes — completed 2026-08-09 (34 production lines removed; 90 net lines added after contracts)

`run-step-walk.ts` now owns single-or-array normalization for every actual-run metadata step, mutually exclusive step-2 discrimination, TTS's character-count gate, shared Whisper path/Reverb label normalization, and typed callbacks for STT, extraction, partial extraction, LLM, TTS, image, video, and music. `computeActualCosts` and `computeActualProcessingTimes` retain only consumer-specific payload construction and select an explicit partial-step policy: cost uses `before-array-stt`, preserving its partial-OCR-before-array-STT serialization, while timing uses `after-step2`, preserving its step-2-before-partial-OCR serialization. Valid run output therefore remains byte-identical without hiding the established ordering fork.

The merged discriminator is unknown-safe and requires exactly one recognized key. A malformed non-array step-2 object with neither `transcriptionService` nor `extractionMethod` is skipped by both consumers, and an ambiguous object carrying both keys is also skipped by both; cost and timing can no longer invent different step lists for either shape. The duplicate `resolveTranscriptionModel` implementations and Whisper regex are gone, and estimated plus actual OCR pool aggregation now call one `computeTotalProcessingTimeMs`. The pre-migration differential records a complete run across array STT, partial OCR, and steps 3–7, byte-compares both serialized projections after adoption, and separately pins malformed/ambiguous omission. The implementation deleted 378 tracked production lines and added 344 including the new shared engine, for 34 net production lines removed; 124 additive contract lines make the landed total 90 lines net additive. The estimate missed the typed visitor/policy boundary and the full serialized fixture needed to preserve the latent ordering fork explicitly. W3.11 had already absorbed reporter's `mapStepEstimate`, so it remains outside this subwave.

### W4.10 — BC-21: stt-resume/ocr-resume shared plumbing, deepened — completed 2026-08-09 (net 30; subsumes backlog R-5)

`provider-batch-resume.ts` now owns all four R-5 mechanical pairs: absolute stored output-directory resolution, `ProviderResumePassResult` projection, source-input validation, and selected-target completeness evaluation. The shared completeness helper receives each domain's stored-target parser and missing-target builder; STT injects its default missing-target policy, while OCR injects `{ includeBlocked: true }`. The byte-identical `file://` parser fallback also moved into the shared module, and the three selected-complete process-result blocks now use one constructor with the exact existing detail and success level.

`priceProviderResumeTarget` now owns manifest loading and validation, parsed-entry traversal, missing-target filtering, estimate collection, and explicit estimate aggregation for STT and OCR. Each domain injects its metadata reader, parser, and estimate builder. The intended policy forks remain in those adapters: STT's work probe and resume path continue to honor `ignoreUnresumableEntries`, its price adapter passes `false` and therefore remains strict, and its YouTube-caption/current-target fallback is unchanged. OCR still silently skips non-record entries, entries without an output directory, and entries without an inferable provider set, but `toStoredSource` still hard-fails with its existing `CLIUsageError` when source information is missing. OCR's remote-document download and cleanup path is unchanged. URL's separate price traversal was deliberately left in place as planned.

The migration deleted 181 production lines and added 151, for 30 net production lines removed. The original ~50 estimate missed the typed price-profile and domain-adapter cost, but the shared boundaries now place source normalization, selection completion, result construction, and price traversal under one implementation without flattening the documented STT/OCR behavior differences.

## Wave 5 — moderate-high-risk consolidations

### W5.1 — BC-1: adopt `runAsyncSttJobLifecycle` in the four async STT runners that still inline the whole lifecycle (net ~280–420, the largest item in this plan; subsumes W1.1/W1.2 structurally; gated: rulings 1 and 2)

This is the design pass the mechanical report explicitly deferred, and the economics have changed since that rejection was written: the engine ALREADY EXISTS at `async-lifecycle.ts:348-551` with rev and speechmatics as production adopters. assemblyai (~135-150 scaffolding lines), gladia (~140-160), soniox (~185-200), and happyscribe (~130-145) hand-roll the identical state machine; supadata (~110) is a hybrid sync/async fifth copy, deferred behind a further human call.

Engine extensions required (the reason the four could not adopt): (1) optional `uploadAsset` stage timed into a new `uploadMs` metric (already in `buildStep2TimingMetadata`'s whitelist), with asset ids persisted into created/progress/completed runtimes; (2) `createJob` receives the upload result; (3) `getTranscript(jobId, metrics, finalStatus)` gains the final poll status so assemblyai/gladia pass an identity getTranscript (their transcript IS the final poll payload — without this, migration adds one HTTP request per run) and happyscribe keeps its two-phase direct-download/export-poll fallback inside its adapter; (4) `cleanup: { shouldDelete(ctx), deleteJob?, deleteAsset? }` replacing the single-resource hooks, writing `remoteJobDeleted`/`remoteAssetDeleted`; (5) engine emits the `logSttSegmentLifecycle` started/completed pair from a `segment` option; (6) a progress-metadata extension hook (happyscribe/supadata persist `billing` into checkpoints; supadata reads it back on resume); (7) the engine finally's `remoteProcessingMs` recompute (`async-lifecycle.ts:513-517`) must subtract the new `uploadMs` (soniox's parallel block already does — a latent formula divergence today); (8) a guard-noun/stage option so per-provider job-creation guard errors keep their current messages/classes. Each provider file becomes rev-shaped: sttStageRequest-based stage functions (soniox's already live in `soniox-api.ts:34-174`), a ~25-line engine config, and a provider `buildResult`. Exported request builders survive where rest-contract tests import them. Keep per-provider `attachError` (wave 12/28 standing guidance — the local attaches deliberately do not unwrap `error.cause`).

Drift this kills beyond W1.1/W1.2: rev/speechmatics silently lost the 'started' segment log event the inline four emit; checkpoint shapes after a crash differ per provider (no hand-rolled runner writes the engine's cleanup-stage crash checkpoint); metrics wiring exists in three styles; final `runtime.stage` differs ('cleanup-complete' vs 'completed' — the restamp is ruling 1's second half). Load-bearing constraint: `classifySttSplitLimitError` and `stt-provider-failures.ts` classify by error-message regex and stage string, so messages must stay byte-stable via `sttStageRequest`'s `failureLabel` mechanism. Migration: soniox → assemblyai → gladia → happyscribe, one provider per commit. Differential harness FIRST: mock-fetch fixture replay byte-comparing result JSON, transcription.txt, and every `writeSttProviderCheckpoint` payload old-vs-new — no full-lifecycle fetch-mock coverage exists today for these five runners.

### W5.2 — BC-16: retire the comic sub-CLI's hand-rolled parser — parse comic through the native parser (net ~120; prerequisite: BC-17 step 1, pulled forward from W6.3; gated: ruling 7)

`comic-utils/cli-args.ts` (475 lines) is a complete second argument parser with its own conventions, and every comic flag is declared twice — parser branches vs help-only tables in `comic-flags.ts` (the file's own comment admits the split), policed only by a ONE-DIRECTIONAL contract test (documented→accepted; a parser branch with no comic-flags entry is silent today). Design: export `parseCommandArgv` from native-parser (pure extraction of lines 343-409), promote the `subcommand-help.ts` definitions (already `Omit<CliCommandDefinition,'handler'>`) into real definitions whose flags ARE the comic-flags tables, replace the three switch loops with per-subcommand `coerceAndValidate` functions keeping all value/cross-flag logic verbatim, and shim errors to the pinned strings. Gating corrections: the native parser CAMELIZES unknown flags (`native-parser.ts:143`) so the original `--panel-limit` spelling is unrecoverable — parseCommandArgv must additionally record original unknown-flag spellings; comic's once-only errors are PINNED ('Grid can only be specified once') and the native parser last-wins losing occurrence counts — hence the prerequisite: land W6.3's step 1 (`rawParsed.flagOccurrences` emission, ~12 additive lines behind the differential gate) before this migration; native accepts `--flag=value` and `--qa=false` which comic rejects today (ruling 7); comic's `-h` handling differs (validation partially runs under -h today — arguably a bug in comic's favor). Tests: cli-usage-errors 632-763 (exact strings), comic-options (rewritten against validators), help contracts. Land the parser export alone first behind the full CLI suites; migrate draft-scenes → generate-images → reference-sketch.

### W5.3 — BC-44: `defineGenerationServiceTest` — one budgeted-live-generation framework for the image/video/music e2e definers + five price-test clones (net ~80)

The three generation service-test definers are one lifecycle (invalid-model gate, output lifecycle, budget gate, env guard, run, artifact+manifest assertions) with drift that matters: the video and music price definers destructure only `{model}` and silently drop the per-model extraArgs consumers pass — e.g. video's veo cases carry `--duration 4` that never reaches the --price command, so the price smoke exercises a different command shape than the live test; image forwards them. Verifier corrections that bound the design: budget-preflight extracts helper keys by CALL NAME from consumer files, so the three named wrappers must survive and consumers must not call the generic directly (or the new name joins `helperBudgetKeySpecs`) — and the budget system fails closed, so key drift breaks tests rather than un-budgeting paid runs; test display names are load-bearing for test-runner report matching and must stay byte-identical; the spec lands at ~15 fields (acceptable for a test framework, but name the tradeoff). TTS/OCR/LLM definers stay — their transient-retry/dual-input logic is genuinely domain-specific. Verification is type-checking + the budget-preflight source-contract diff (the covered suites are paid and must not run) — which is why this sits in the moderate-high wave despite touching only test code.

## Wave 6 — high-risk consolidations

### W6.1 — BC-3: delete OCR "pipeline A" — converge rendered-page hosted OCR onto the chunk-fallback engine (net ~170; subsumes W1.3 by deletion; gated in part: ruling 4)

step-2-ocr contains two complete resumable per-page pipelines writing the byte-identical on-disk layout (`page-results/page-%06d.{json,txt}`, `partial-extraction.txt`): pipeline A (`hosted-ocr-utils.ts:111-240,298-359` + `pdf-page-rendering.ts` + `page-concurrency.ts`, used only by the kimi/grok/deepinfra document branches of `hosted-ocr.ts`) and pipeline B (`pdf-chunk-fallback*.ts`, `runHostedOcrWithPdfChunkFallback`, used by everything else and already supporting rendered-PNG page chunks via gemini's `createGeminiOcrPageImageChunk`). `ocr-partial-step2.ts:75-144` carries dual cache parsers solely because two payload schemas share one directory layout. Design: generalize gemini's PNG chunk creator into an exported `createRenderedPngPageChunk(dpi, ocrPreparationCache)` (fold in the preparation-cache integration pipeline B lacks; the creator must copy the cached render to the engine's `chunkPath`), add a forced-page-mode entry for image-only providers, rewire kimi → grok → deepinfra through `runChunkableHostedPdfOcr`, then delete pipeline A (~196 lines of hosted-ocr-utils + 93 pdf-page-rendering + 47 pool + 35 parseRenderedPageCache + the stored-page type). Beyond W1.3, this fixes pipeline B's weak cache validation (no model/sourceFile check — can silently resume pages cached under a different model in single-target flows) in one place. The `isPageResult` confidence-clause drift cited by the original plan was already closed by mechanical O-3 and is no longer a W6.1 benefit. Observable deltas a maintainer must accept: cached-page log message+level change (pinned by `rendered-hosted-pages.test.ts:119,198-202` — test updates required); `providerUsage` shifts from one document-level entry to per-page `unit:'chunk'` entries (top-level token totals preserved); page PNGs appear under the provider dir; in-flight kimi/grok/deepinfra resume artifacts need a read-side compat adapter (one-release window). Tests: both pipelines have strong pure-local mock suites; byte-diff harness across old-A/new-B is directly buildable. Provider-at-a-time revertible. Follow-up unlocked: a table-driven hosted-OCR provider registry becomes near-mechanical after this lands (selection precedence must become registry order).

### W6.2 — BC-18: one provider-run state machine for stt-run-state / ocr-run-state / url-run-state, staged (net ~45 stage 1, ~100 full; subsumes W1.10/W1.11 structurally)

Three copies of one state machine decide what resume re-runs and what exit code a batch gets, and they have drifted in ways that change persisted manifests (the now-fixed W1.10/W1.11 behavior, plus: STT validates `transcriptionService` against its registry set while OCR accepts any string; OCR hard-throws on malformed stored provider metadata where STT silently skips; URL's parser tolerates missing artifactDir/attempts both siblings reject). Stage 1 (safe, net ~40-45): extract the domain-independent pure algebra into `step-2-shared/provider-batch-state.ts` over a minimal structural `ProviderStateLike` — the completion-status trio, missing/blocked builders, parse-loop skeletons, and a shared exit-2 `ProviderBatchCompletionError` base; exclude the `readExistingRun` skeletons (highest hook count, least shared content). OCR keeps blocked-state policy, STT keeps skipped/local/diarization handling, injected as predicates. Stage 2 (full codec engine, net ~100 total): `createProviderRunState<TTarget,TState,TStoredError>(codec)` with a ~12-member codec + 5-hook policy object — the verifier warns this flirts with the rejected mega-helper shape and mostly relocates lines; its real deliverable is a differential harness enforcing the resolved completion fallback (all functions are pure over `Record<string,unknown>`, so old-vs-new byte-identical JSON over recorded fixtures is cheap). The URL leg is marginal on lines (~0-20; its buildProviderStates consumes outcome arrays, needing an adapter). Execute stage 1, preserve W1.10's landed attempts/artifact/skip semantics and W1.11's requested-membership fallback, and let stage 2 stand or fall on them. Tests: the additive resume-provider contract pins the STT reconciliation semantics, while ocr-resume-contracts pins the OCR functions directly.

### W6.3 — BC-17: one canonical parse representation — delete the argv-mirror track in selector normalization (net ~65; step 1 may already be landed by W5.2)

Every selector rule is written twice — once against the parsed flags map, once as an argv rewrite — because `buildOptsFromFlags` re-parses raw argv (`build-options-from-flags.ts:34-35,70-74,152`) and `config-merge.ts:73-88` re-derives explicit flags from `Bun.argv`. The two tracks have already drifted three ways, all latent only because the flags pass happens to run first: suppressed-selector-value handling exists args-side only; the single-URL-backend conflict check exists flags-side only; write-step-selectors mirrors provider selectors but not the --all-providers/--all-local expansion. Design: step 1 — parser emits `rawParsed.flagOccurrences` in encounter order (~12 lines; done under W5.2 if that ran first); step 2 — `buildOptsFromFlags` consumes occurrences behind the differential gate (wave-14 style, 400+ argv cases, everything pure); step 3 — each normalize rule is written ONCE as occurrence→occurrences[] with map/explicitFlags updates derived through one generic applier (the "update alongside" option would rebuild the dual-write — reject it; and the map cannot be fully replayed from occurrences because config merge injects into the map only); step 4 — delete the entire args track (~100-110 lines) and the `Bun.argv.slice(2)` plumbing in five define-commands. Must-preserve semantics: raw-argv values take precedence over config-injected map values in tts-options (lines 58, 74-75, 80, 104, 119); occurrence lists are pre-config-merge by design. Tests: generic-selector-normalization pins exact rawArgs arrays and will be rewritten; provider-expansion-concurrency pins occurrence-order-dependent model lists. The parse core touches every command — the harness is the mitigation.

## Examined and not recommended (do not re-litigate without new evidence)

One engine spanning STT and media generation — rejected: the STT lifecycle is checkpointed/resumable, media polling is stateless fire-and-poll; the right architecture is two engines by persistence class (W5.1, W4.4) sharing retry primitives. One resume engine across all six domains — rejected: single-run (generation) and per-entry batch (provider-batch) are genuinely different shapes; the right end state is two engines with domains adopted per shape (W4.8 starts this). The wave-9-excluded concurrency pools as one richer pool — rejected; the honest remainder is `{failFast, onItemDone}` options on `mapWithConcurrency` (~50 net, mechanical ride-along). An AIMD concurrency-gate extraction for the TTS/OCR chunk/lane schedulers — verified structural duplication (~145 kernel lines, plus a drifted third sibling in the test runner), but the corrected net is negative once the gate's required operations (pause-without-halve, external cap mutation, cap-change eventing) are priced; file ruling 13's arithmetic items instead. Full merge of SttBatchCoordinator with the scheduler family — its slot/cooldown/degrade algebra is load-bearing batch semantics. The per-provider `collect*Targets` files (~25 files) — provider-specific wiring; a registry is a config-object trap. Hosted write-runner family beyond W3.9 — already consolidated onto two compat engines. WebArticleMetadata assignment ladders — value sources genuinely differ per provider (though the key-order objection is now disproven — nothing pins run.json key order). run-ocr.ts input-family ladder, glm/mistral OCR runners, ocr-services config files, pdf/ocr-chapters — genuinely distinct or already thin. TTS directory batch vs processBatch — scheduler-incompatible execution models; only a ~25-net tally slice is extractable. Metadata process-target double-resolution (directory scans and up-to-3-network-probe routing run twice per invocation) — real but sub-threshold; fix directly by memoizing, not by redesign. macos-managed-tools build flows (~25 net), model-loader cents/USD ladders (~20), `usdOrCents` (~15), run-benchmark computeSummary twins (~25), aggregate-timing ladders (~20), buildComicPagePromptData/buildSketchPromptData twins (~15), defuddle runner, links command, src/prompts, structured-output — all below the redesign bar; ride-alongs at best. SUPPORTED_*_MODELS TS lists vs models/*.json registries — verified currently in sync, cannot be derived at compile time; recommend a CI contract test asserting list == registry keys per domain instead of consolidation. The umbrella "two estimated-cost pipelines" problem (StepEstimate vs EstimatedStepEntry) — real, but a full merge touches every preflight consumer; flagged for an ADR-level design pass, with W4.3/W3.11 as the tractable increments. generate-scene-sketches and location-reference-command QA cousins — different lifecycles from the comic page/panel pair; no unification recommended. price-mode-contracts and grouped-tier assertion tails — domain-specific assertions; only the fixture side paid in W3.6.

## Method and provenance

Produced by a 27-agent adversarial workflow on 2026-08-09: 11 subsystem analysts (STT hosted services, OCR, generation steps 4–7, local runtimes, CLI layer, resume/run-state, URL+download, pricing/benchmark, comic+renderers, the test suite, and a cross-cutting architecture pass), 11 paired adversarial verifiers that re-read every citation and re-did every count, one completeness critic sweeping unowned areas, and 4 verifiers for the critic's proposals — ~4.8M tokens of analysis, ~1,150 tool calls, all read-only. Of 48 verified findings, none was refuted outright; a review pass narrowed the program to the 31 consolidations in Waves 2–6, and the cut findings' live bugs are preserved as Wave 1 subwaves marked "standalone". Verification techniques: scratchpad extract-and-diff for byte-identity claims, throwaway schema-diff scripts (the W3.12 ladder audit), grep-audits of `test/` for every pinned string cited, import-cycle checks for proposed helper homes, and cross-checks against `duplication-report.md`'s landed waves, backlog, dropped items, and rejected list. Mechanical U-2 and W-1/W-2 are retired; W3.5 completed U-2's remaining behavioral scope, and W4.2 completed W-3's remaining scope. Wave 2 landed backlog items TT-4 and TE-2, W3.1/W3.2/W3.6 landed TE-3/TE-4/TE-7, W3.8 landed X-6, W4.1 landed B-6, W4.2 landed W-3, W4.8 landed R-4, and W4.10 landed R-5. No backlog ID remains subsumed by the unimplemented plan.
