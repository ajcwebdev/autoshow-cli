{
  "whisperfile": {
    "description": "Local speech-to-text via prebuilt Mozilla whisperfile bundles",
    "type": "local",
    "models": {
      "tiny": {
        "description": "Tiny — fastest, lowest accuracy",
        "costPerHourUSD": 0,
        "costPerHourCents": 0,
        "limits": {
          "notes": "Local whisperfile transcription runs on the local machine rather than a hosted API, so provider-side upload and duration caps do not apply."
        },
        "estimation": {
          "msPerSecond": 33
        }
      },
      "tiny.en": {
        "description": "Tiny English-only — fastest, lowest accuracy",
        "costPerHourUSD": 0,
        "costPerHourCents": 0,
        "limits": {
          "notes": "Local whisperfile transcription runs on the local machine rather than a hosted API, so provider-side upload and duration caps do not apply."
        },
        "estimation": {
          "msPerSecond": 33
        }
      },
      "small": {
        "description": "Small — balanced speed and accuracy",
        "costPerHourUSD": 0,
        "costPerHourCents": 0,
        "limits": {
          "notes": "Local whisperfile transcription runs on the local machine rather than a hosted API, so provider-side upload and duration caps do not apply."
        }
      },
      "small.en": {
        "description": "Small English-only — balanced speed and accuracy",
        "costPerHourUSD": 0,
        "costPerHourCents": 0,
        "limits": {
          "notes": "Local whisperfile transcription runs on the local machine rather than a hosted API, so provider-side upload and duration caps do not apply."
        }
      },
      "medium": {
        "description": "Medium — good accuracy, moderate speed",
        "costPerHourUSD": 0,
        "costPerHourCents": 0,
        "limits": {
          "notes": "Local whisperfile transcription runs on the local machine rather than a hosted API, so provider-side upload and duration caps do not apply."
        }
      },
      "medium.en": {
        "description": "Medium English-only — good accuracy, moderate speed",
        "costPerHourUSD": 0,
        "costPerHourCents": 0,
        "limits": {
          "notes": "Local whisperfile transcription runs on the local machine rather than a hosted API, so provider-side upload and duration caps do not apply."
        }
      },
      "large-v2": {
        "description": "Large v2 — highest accuracy, slowest",
        "costPerHourUSD": 0,
        "costPerHourCents": 0,
        "limits": {
          "notes": "Local whisperfile transcription runs on the local machine rather than a hosted API, so provider-side upload and duration caps do not apply."
        },
        "estimation": {
          "msPerSecond": 145
        }
      },
      "large-v3": {
        "description": "Large v3 — highest accuracy, slowest",
        "costPerHourUSD": 0,
        "costPerHourCents": 0,
        "limits": {
          "notes": "Local whisperfile transcription runs on the local machine rather than a hosted API, so provider-side upload and duration caps do not apply."
        },
        "estimation": {
          "msPerSecond": 145
        }
      }
    }
  }
}
