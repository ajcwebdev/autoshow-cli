{
  "whisper": {
    "description": "Local speech-to-text via Whisper.cpp",
    "type": "local",
    "models": {
      "tiny": {
        "description": "Tiny — fastest, lowest accuracy",
        "costPerHourCents": 0,
        "limits": {
          "notes": "Local whisper.cpp transcription runs on the local machine rather than a hosted API, so provider-side upload and duration caps do not apply."
        },
        "estimation": {
          "msPerSecond": 33
        }
      },
      "base": {
        "description": "Base — fast with reasonable accuracy",
        "costPerHourCents": 0,
        "limits": {
          "notes": "Local whisper.cpp transcription runs on the local machine rather than a hosted API, so provider-side upload and duration caps do not apply."
        },
        "estimation": {
          "msPerSecond": 48
        }
      },
      "small": {
        "description": "Small — balanced speed and accuracy",
        "costPerHourCents": 0,
        "limits": {
          "notes": "Local whisper.cpp transcription runs on the local machine rather than a hosted API, so provider-side upload and duration caps do not apply."
        }
      },
      "medium": {
        "description": "Medium — good accuracy, moderate speed",
        "costPerHourCents": 0,
        "limits": {
          "notes": "Local whisper.cpp transcription runs on the local machine rather than a hosted API, so provider-side upload and duration caps do not apply."
        }
      },
      "large-v3-turbo": {
        "description": "Large v3 Turbo — fast with near-v3 accuracy",
        "costPerHourCents": 0,
        "limits": {
          "notes": "Local whisper.cpp transcription runs on the local machine rather than a hosted API, so provider-side upload and duration caps do not apply."
        },
        "estimation": {
          "msPerSecond": 145
        }
      }
    }
  }
}
