{
  "spider": {
    "description": "Spider hosted article extraction",
    "type": "api",
    "models": {
      "spider": {
        "description": "Spider /scrape markdown extraction heuristic estimate",
        "pricingSourceUrl": "https://spider.cloud/",
        "pricingCheckedAt": "2026-05-31",
        "pricingCurrency": "USD",
        "pricingTier": "Base/reference heuristic",
        "pricingNotes": "Spider pricing can depend on scrape mode, compute, and bandwidth. AutoShow keeps a conservative base/reference per-page heuristic for dry-run totals; free credits, discounts, enterprise pricing, taxes, and provider credits are ignored.",
        "costPer1kPagesUSD": 1.2,
        "costPer1kPagesCents": 120,
        "limits": {
          "notes": "Spider publishes usage billing based on bandwidth and compute rather than a fixed per-page scrape price. AutoShow uses a conservative heuristic per-page estimate for dry-run totals."
        },
        "estimation": {
          "costMultiplier": 1,
          "msPerPage": 3500
        }
      }
    }
  }
}
